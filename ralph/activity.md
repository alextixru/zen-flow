# Activity log — piece amoCRM (Ralph-цикл)

Хронологический лог итераций. Одна запись на итерацию/задачу. Новые записи — сверху вниз в порядке выполнения.

### 2026-07-11 — T038: Spike — ре-пауза (DELAY-цикл) в piece-экшене
- Статус: done — **ВЕРДИКТ: ре-пауза РАБОТАЕТ** (подтверждено живым прогоном на локальном стенде).
- Код в коммит НЕ входит: временно правил `amocrm/src/lib/actions/wait-for-task-completed.ts` (self-driving DELAY-цикл: BEGIN → DELAY-waitpoint 40с; каждый RESUME при `pauses<=2` → новый DELAY-waitpoint 40с (ре-пауза), иначе return) и `amocrm/src/lib/auth.ts` (validate → `{valid:true}`, чтобы создать connection без живого вызова amo). Обе правки откачены `git checkout --`; `git status` чист, токен-чек на утечку JWT (`git diff` по префиксу токена) = 0.
- **Разбор кода (почему движок ре-паузу не запрещает):** `piece-executor.ts` на RESUME (`isPaused===true` → `ExecutionType.RESUME`) вызывает тот же `pieceAction.run` (строки 152-153); если `run()` зовёт `createWaitpoint`+`waitForWaitpoint`, хук ставит `hookResponse.type='paused'` (createWaitForWaitpointHook), и дальше вердикт `FlowRunStatus.PAUSED` выставляется одинаково для BEGIN и RESUME (строки 185-189) — гварда против повторной паузы того же шага нет. Сервер: `waitpoint-service.createForPause` создаёт СВЕЖИЙ waitpoint на каждый вызов и для DELAY планирует системный job `RESUME_DELAY_WAITPOINT` с `jobId: resume-delay-${flowRunId}` (ключ по flowRun; к моменту ре-паузы прошлый job уже отработал, поэтому пере-планирование с тем же id проходит).
- **Живой прогон (стенд `scripts/start-ce.sh`, порт 8080 + отдельный worker-процесс на 8082, `AP_DEV_PIECES=amocrm`):** собрал flow (catch_webhook trigger + amocrm `wait_for_task_completed`) через `IMPORT_FLOW`, запустил тестовый прогон через socket.io `TEST_FLOW_RUN`. Наблюдённые переходы `GET /flow-runs/:id` (run `bz3m5NIgBZeBuZwIBngg9`):
  - PAUSED, `step_1.output={phase:'begin', pauses:1}` — первая пауза (BEGIN).
  - → (через ~40с DELAY-резюм) PAUSED, `{phase:'repause', pauses:2}` — **РЕ-ПАУЗА #1**.
  - → (ещё ~40с) PAUSED, `{phase:'repause', pauses:3}` — **РЕ-ПАУЗА #2**.
  - → (ещё ~40с) SUCCEEDED, `{phase:'done', pauses:3, completed:true}`.
  - Логи app подтверждают: 3× `POST /api/v1/waitpoints` (principalType ENGINE) и 3× `[RESUME_DELAY_WAITPOINT] Resuming flow` по одному и тому же `flowRun.id` с интервалом ~40с (11:07:37 / 11:08:19 / 11:09:00). То есть один и тот же шаг паузился DELAY-waitpoint'ом, резюмился и создавал новый DELAY-waitpoint дважды подряд, затем штатно завершился.
- **Инфра-заметки (важно для T039, записаны в память `local-preview-setup`):** `ce-preview.env` ставит `AP_CONTAINER_TYPE=APP` — при этом API отвечает, но НЕ исполняет worker-джобы (валидация connection и прогон flow висят в BullMQ `prioritized`, POST /app-connections таймаутит). Решение: поднять ОТДЕЛЬНЫЙ `packages/server/worker/dist/src/bootstrap.js` (`AP_CONTAINER_TYPE=WORKER AP_PORT=8082 AP_FRONTEND_URL=http://localhost:8080 AP_DEV_PIECES=amocrm AP_WORKER_TOKEN=<jwt из AP_JWT_SECRET>`) — воркер тянет джобы у app по socket.io при `AP_REDIS_TYPE=MEMORY`. `WORKER_AND_APP` в одном процессе не помог.
- Верификация: сам прогон. После отката — `npx turbo run build --filter=@activepieces/piece-amocrm` зелёный (оригинал собирается), `git status` чист.
- Блокеры: нет. **T039 РАЗБЛОКИРОВАНА** (условие «T038=работает» выполнено): следующая итерация может переводить `wait_for_task_completed` и `wait_for_customer_reply` на DELAY-поллинг.

### 2026-07-11 — T037: Универсальный триггер event_occurred
- Статус: done
- Контекст итерации: предыдущая итерация реализовала T037 целиком, но была прервана до коммита и записи в лог — рабочее дерево содержало незакоммиченные `event-occurred.ts`, регистрацию в `triggers/index.ts`, i18n-ключи и уже проставленный чекбокс T037 в `prd.md`. Эта итерация: сверка реализации со спекой, полная верификация, коммит, запись в лог. Кода не дописывал — реализация соответствовала спеке.
- Изменения (от прошлой итерации, приняты как есть): `src/lib/triggers/event-occurred.ts` — trigger `event_occurred` через poll-фабрику `createAmoEventsPollingTrigger` (T030). `typesFromProps` → `selectedTypes(propsValue.event_types)` (guard: только строки из массива); `filterEvent` → `matchesEntity` (пропуск при any/пусто, иначе `event.entity_type === entity`). Props: `event_types` `Property.MultiSelectDropdown` (required, options из `amoEvents.fetchEventTypes`, **per-field типы `type===24` исключены** через `PER_FIELD_TYPE_CODE`, label `lang`, value `key`, ponytail-коммент про причину), опц. `entity` StaticDropdown Any/lead/contact/company/task. sampleData — реальное событие стенда (lead_status_changed, id ULID). Description по спеке. Регистрация в `triggers/index.ts` (импорт + массив `amocrmTriggers`). i18n +3 identity-ключа (Event Occurred + описание, Event Types-описание, entity-описание). Без any/as, named params, консты/типы в конце файла.
- Команды: `npx turbo run lint build --filter=@activepieces/piece-amocrm` — pass (6/6, lint cache-hit из прошлой итерации, build force-executed зелёный); `npm run lint-dev` — exit 0, 30/30 tasks, 0 errors (те же 72 предсуществующих web-warning вне скоупа); токен-чек на утечку JWT (`git diff | grep -c` по префиксу токена) = 0 (и в новом файле 0).
- Верификация: pass. Живой smoke `test()`-пути на dev-стенде (спека требует «мультиселект из 2-3 типов отдаёт реальные события»): `GET /events?filter[type][]=entity_linked&filter[type][]=lead_status_changed&filter[created_at][from]=<now-7д>&limit=3` → **HTTP 200**, n=3 (lead_status_changed lead + два entity_linked lead/company) — форма запроса ровно та, что строит `fetchEvents` из `test()` при `typesFromProps` мультиселекте. Per-field исключение (type 24) — по подтверждённому в T029/T034 факту, что per-field типы несут `type:24`. Самопроверка диффа: файл — тонкая обёртка над уже провалидированной poll-фабрикой T030 (зеркало потребителей T035/T036), новая чистая логика (`selectedTypes`/`matchesEntity`) тривиальна, отдельный юнит-тест не нужен (спека `verify` его не требует, курсор покрыт `events.test.ts`). Скилл `code-review` не гонял отдельно: дифф — один trigger-файл фабричного паттерна без новой сетевой/арифметической логики, находок нет по чтению.
- Блокеры: нет.
- Наблюдение (вне скоупа): в рабочем дереве присутствует untracked-директория `amocrm-app/` (README/RESEARCH.md/research) — вне разрешённых путей цикла, в коммит НЕ включена, не трогал.

### 2026-07-11 — T034: Action find_events (история изменений сущности)
- Статус: done
- Изменения: `src/lib/actions/find-events.ts` (action `find_events`, aiMetadata `idempotent:true`, description «Audit history: who changed what and when…»). Props: `entity_type` StaticDropdown Any/leads/contacts/companies/tasks (optional), `entity_id` через `linkedEntityDropdown({typeProp:'entity_type', required:false})`, `event_types` `Property.MultiSelectDropdown` (options из `amoEvents.fetchEventTypes` — label `type.lang` локализованный, value `type.key`), `created_by` через `userDropdown({required:false})`, `from`/`to` `Property.DateTime` (optional), `limit` Number default 50. `run`: валидация per-field эксклюзивности (regex `^custom_field_\d+_value_changed$`; если выбран per-field-тип И длина>1 → читаемый `throw`), маппинг `entity_type` plural→singular (`leads→lead`…`tasks→task`, `any`/undefined → без фильтра) через const `ENTITY_FILTER`, `toEpochSeconds` (ISO→сек, NaN-guard), вызов `amoEvents.fetchEvents({types, entity, entityIds, createdBy, from, to, limit: min(limit,100), maxPages:1})` → массив событий как есть (204→`[]` из fetchEvents). Регистрация в `actions/index.ts` (24-й action, после findEntity). i18n +11 identity-ключей (Find Events + описание, Event Types + описание, From/To + описания, Any, Task, limit-описание). Без any/as, named params, хелпер/консты в конце файла.
- Решения: (1) `entity_type` значения оставлены plural (leads/contacts/companies) — чтобы работал refresher `linkedEntityDropdown` (`entityByTypeDropdown` проверяет plural); для events-фильтра `filter[entity][]` требуется singular (подтверждено живьём в T029/этой итерации: `filter[entity][]=lead`/`task`) — маппинг plural→singular в `run`. (2) per-field детект по key-паттерну (в run доступны только строки-keys, не `type:24`); generic `custom_field_value_changed` НЕ матчит (нет цифр) → комбинируется свободно, эксклюзивен только per-field. (3) `maxPages:1` + `limit≤100` — одна страница до `limit` событий (потолок спеки 100). (4) `created_by`/`event_types` реюз `userDropdown`/`fetchEventTypes` — новых дропдаунов не заводил.
- Команды: `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5, типы `entityIds`/`createdBy` из дропдаунов компилируются без каста); `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 30/30 tasks, 0 errors (те же предсуществующие web-warning вне скоупа). Токен-чек: токен-чек на утечку JWT (`git diff` по префиксу токена) = 0. Юнит-тест не добавлял: чистой арифметики нет (`toEpochSeconds` тривиален, per-field regex — одна строка), курсорная логика уже покрыта `events.test.ts` (T029), fetchEvents/пагинация — `client`-паттерном.
- Верификация: pass. Живой smoke на dev-стенде (спека требует): `GET /events?filter[entity][]=lead&filter[entity_id][]=36632537&page=1&limit=50` → **200**, n=50, все `entity_id=36632537` (типы: common_note_added, sale_field_changed, `custom_field_829516_value_changed`, lead_status_changed, entity_tag_*, entity_*linked); `filter[entity][]=task` → 200 (2 события entity_type=task); `filter[created_by][]=0` → 200 (n=2, all_by {0} — на стенде все события системные created_by:0, но форма фильтра подтверждена); `GET /events/types?language_code=ru` → 218 типов, `lang` локализован («Новая сделка»…) — форма для MultiSelectDropdown label совпала. Полная последовательность `run()` (маппинг entity+entityIds+types) выверена этим replay. code-review (skill, low) по диффу: 1 находка PLAUSIBLE/low — при `entity_type=tasks` `entity_id` через `linkedEntityDropdown` невыбираем (фабрика поддерживает только leads/contacts/companies), т.е. история конкретной задачи по id недоступна, только account-wide по `filter[entity][]=task`. Это буква спеки (`entity_id (linkedEntityDropdown …)`) — не баг, фикса не требует (entity_id optional).
- Блокеры: нет.

### 2026-07-11 — T030: Poll-фабрика createAmoEventsPollingTrigger
- Статус: done
- Изменения: `src/lib/common/events-polling.ts` — `createAmoEventsPollingTrigger({name, displayName, description, aiMetadata, types|typesFromProps?, entity?, props?, sampleData})` → `createTrigger` c `TriggerStrategy.POLLING` (форма опций зеркалит webhook-фабрику T004). `onEnable`: `store.put('cursor', {lastCreatedAt: nowSeconds, lastIds: []})` + `context.setSchedule({cronExpression: '* * * * *'})` (метод есть в POLLING-контексте фреймворка `context/index.ts:88` — дефолт платформы 5 мин перебит на минуту, лезть в ядро не пришлось). `run`: читает курсор (нет → инициализирует now, `[]`), `amoEvents.fetchEvents({from: cursor.lastCreatedAt, types, entity})`, `amoEvents.advanceCursor`, пишет `next`, возвращает `fresh` (каждое событие — item; курсор двигается только по фактически вернувшимся событиям). `test`: `fetchEvents({from: now-7д, limit:5, maxPages:1, types})` без трогания курсора, пусто → `[sampleData]`. `resolveTypes` даёт приоритет `typesFromProps` над `types`. Хелперы (`resolveTypes`/`initialCursor`/`nowSeconds`) вне const, типы/константы в конце файла, без any/as. Реэкспорт в `common/index.ts`.
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5, типы `StaticPropsValue<InputPropertyMap>` компилируются без каста); `npm run lint-dev` — exit 0, 0 errors (те же 72 предсуществующих web-warning вне скоупа). Токен-чек: `git diff --cached | grep -c eyJ0` = 0.
- Верификация: pass (спека: «общий; фабрика без потребителя проверяется с T035/T037; типы без any/as»). Юнит-тест не добавлял: чистая логика курсора уже покрыта `events.test.ts` (T029), `run()` новой чистой арифметики не вносит (`nowSeconds`/`initialCursor` тривиальны). Живой smoke по спеке не требуется — у фабрики пока нет потребителя. Самопроверка диффа: соответствие спеке попунктно, находок корректности нет.
- Наблюдение (вне скоупа T030, к V006/T029): при DESC-порядке ленты (подтверждён в T029) и упоре в потолок `maxPages` (500 событий/тик) `advanceCursor`→MAX(created_at) продвинет курсор на новейшее из вернувшихся, а более старые НЕвыбранные события той же дельты окажутся `< lastCreatedAt` и потеряются — т.е. спековая формулировка «при упоре в потолок события доберутся следующим тиком» неверна для DESC. На практике недостижимо (1-мин поллинг при ~7 rps аккаунта = ≪500 событий/мин), потолок — предохранитель; но это свойство закоммиченного `advanceCursor` (T029), а не фабрики T030 — не чинил (правило скоупа), фиксирую для чекпоинта V006.
- Блокеры: нет.

### 2026-07-11 — T029: common/events.ts — клиент Events API + курсорная механика
- Статус: done
- Изменения: `src/lib/common/events.ts` (`amoEvents = { fetchEvents, fetchEventTypes, advanceCursor }`: `fetchEvents` строит query через `URLSearchParams` с брекет-фильтрами, пагинация page++ до отсутствия `_links.next`/пустой страницы, жёсткий потолок `maxPages` default 5 = 500 событий, limit clamp ≤100; `fetchEventTypes` → `_embedded.events_types` → `{key,type,lang}`; `advanceCursor` — чистая функция курсора `{lastCreatedAt, lastIds[]}`, дедуп на границе секунды). Разбор ответов через `unknown` + гварды (`isRecord`/`toEvent`/`readEvents`), без any/as; типы/константы в конце файла. Колокейт `events.test.ts` (9 тестов), реэкспорт в `common/index.ts`.
- **Живой smoke (dzenteamdev, зафиксировано):**
  - `GET /events?limit=3` — **id = ULID-строка** (`01kx7sa7jyfhdsm4s7wsmm1t51`), `created_at` в секундах, **порядок desc** по created_at; `_embedded.events`, `_links.next` присутствует. Событие несёт `type`, `entity_id`, `entity_type`, `created_by`, `value_after`/`value_before` (массивы), `account_id`, `oauth_client_uuid`, `_embedded.entity`.
  - `GET /events/types?language_code=ru` — embeddedKey **`events_types`** (не `types`), `_total_items` присутствует, **`_links.next` отсутствует → не пагинируется** (одна страница). **218 типов**: 102 стандартных + 116 per-field (`type: 24`, ключи вида `custom_field_788891_value_changed`, плюс общий `custom_field_value_changed`). Ключевые key: `lead_added`(1)/`lead_deleted`(7)/`lead_restored`(10)/`lead_status_changed`(14)/`lead_linked`(80)/`lead_unlinked`(84)/`contact_added`(2)/`company_added`(3).
  - `filter[entity][]=lead&filter[entity_id][]=<id>` — работает, сужает до сущности (3 события common_note_added одной сделки).
  - `filter[type][]=lead_status_changed&filter[type][]=lead_added` (два типа) — работает без ошибки.
  - **`filter[created_at][from]` ВКЛЮЧИТЕЛЕН (>=)**: from = created_at последнего события → это событие вернулось. Значит секундный перехлёст курсора (`from = lastCreatedAt`) покрывается `lastIds`-дедупом — реализовано.
  - **Percent-encoded скобки** (`filter%5Btype%5D%5B%5D=`) amo принимает — `URLSearchParams` не ломает фильтры (проверено живьём).
- Команды: `npx vitest run .../events.test.ts` — 9/9 passed; `npx turbo run lint build --filter=@activepieces/piece-amocrm` — pass (6/6); `npm run lint-dev` — 0 errors (те же 72 предсуществующих web-warning). Токен-чек: `git diff --cached | grep -c eyJ0` = 0.
- Верификация: pass. Code-review (самопроверка диффа) нашёл **баг в `advanceCursor`**: при неизменной граничной секунде новый курсор пересобирал `lastIds` только из `fresh`, теряя уже-эмитнутый id → на следующем поллинге старое событие в ту же секунду переэмитилось. Исправлено: старые `lastIds` переносятся при `lastCreatedAt === cursor.lastCreatedAt` (union через Set); добавлен покрывающий тест. `test-unit` не гонял целиком — корневой раннер piece-тесты не подхватывает (факт репо из T002), достаточно локального vitest.
- Блокеры: нет.

### 2026-07-11 — V005: чекпоинт-валидация блока T022–T027
- Статус: done (фиксов кода не потребовалось)
- Изменения: только `ralph/prd.md` (чекбокс V005) и эта запись.
- Команды: `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (72 предсуществующих warning в web); `npm run test-unit` — engine/shared зелёные, падают те же 8 предсуществующих web-тестов (`output-table-view.test.ts`, `utils-schema.test.ts`), что зафиксированы в V001 — ветка web не трогает, чинить нельзя (вне разрешённых путей); `npx vitest run packages/pieces/community/amocrm/src` — 29/29 pass (5 файлов, включая новый `copy-lead.test.ts` 4/4); `git status` — чисто.
- Структура: все 24 action- и 19 trigger-файлов зарегистрированы в своих `index.ts` и через `amocrmActions`/`amocrmTriggers` в `src/index.ts` (скриптовая сверка, 0 пропусков); 43 уникальных `name`, дублей нет; grep `: any|as [A-Z]` вне тестов — 0; i18n — 0 пропусков среди displayName/description/placeholder (скриптовая сверка; `aiMetadata.description` в i18n не входит — сверено с паттерном kommo).
- Скоуп: `git diff main` вне разрешённых путей — только файлы chore(ralph)-коммитов оркестратора (`.agents/designs/`, `.claude/settings.json`, `.claude/scheduled_tasks.lock`, `.gitignore`) — те же, что зафиксированы в V001; piece-коммиты блока чистые.
- Сверка со спеками: sonnet-верификатор (свежий контекст) прошёл T022–T027 попунктно — **все шесть соответствуют**. Три замечания, ни одно не требует фикса: (1) средняя/архитектурная — T026/T027 регистрируют новую webhook-подписку на КАЖДЫЙ запуск action; при большом числе одновременно ожидающих flow-запусков возможно приближение к лимиту ~100 вебхуков на аккаунт (в ponytail-комментах кода описан account-wide резюм, но не сценарий накопления подписок) — кандидат на апгрейд (одна общая подписка + маршрутизация по waitpoint id), спека этого не требовала; (2) T024 — имена пропсов `pipelineId`/`statusId` вместо `target_*` из буквы спеки — осознанное решение ради refresher'а `statusDropdown`, задокументировано ещё в записи T024; (3) T026/T027 — `createWaitpoint`/`waitForWaitpoint` вместо названных в спеке `run.pause`/`generateResumeUrl`: последние помечены `@deprecated` во фреймворке (`packages/pieces/framework/src/lib/context/index.ts`) — реализация правильнее буквы спеки (правило «No deprecated APIs»). Отдельно подтверждено: waitpoint-API — штатный (используется core-pieces approval/delay/webhook), сборка не зависит от Chats API; каскад T023 батчит PATCH по 250 (лучше допущенного спекой минимума). Скилл `code-review` (low) по диффу блока df1f56c7..HEAD — находок нет.
- Блокеры: нет. Токен-чек: токен-чек на утечку JWT (`git diff` по префиксу токена) = 0.

### 2026-07-11 — T027: ожидание ответа клиента (waitpoint)
- Статус: done (функциональный webhook-waitpoint — Chats API доступен на уровне подписки на события)
- Изменения: `src/lib/actions/wait-for-customer-reply.ts` (action `wait_for_customer_reply`, props `{}`): двухфазный `run` по `context.executionType` — **BEGIN**: `context.run.createWaitpoint({type:'WEBHOOK'})`, регистрирует amo-вебхук `POST /webhooks {destination: waitpoint.buildResumeUrl(), settings:['add_message']}`, кладёт `{resumeUrl}` в `context.store` (ключ `wait_for_customer_reply:{run.id}:{step.name}`), `waitForWaitpoint(id)`, возвращает `{received:false}`; **RESUME**: читает store, best-effort `DELETE /webhooks {destination}` через `tryCatch`, извлекает сообщение из `context.resumePayload.body` по пути `message.add[0]` (локальный `extractMessage`+`isRecord` guard, без any/as), возвращает `{received:true, message}`. Регистрация в `actions/index.ts` (+waitForCustomerReply, после waitForTaskCompleted); i18n +2 identity-ключа (displayName + description). Зеркало T026 минус создание задачи.
- Решение: событие подписки — `add_message` (то же, что триггер T011 `incoming_message`, `payloadPath: message.add`), entityType talks. Props пустые: вебхуки amo account-wide без фильтра по разговору/контакту — резюм на ПЕРВОМ входящем сообщении в аккаунте (тот же потолок, что у T026 `update_task`); фильтр по контакту потребовал бы ре-паузы (вне штатных примитивов) — задокументирован `ponytail:`-комментом. Точный per-contact резюм — trigger/DP-side связка, вне скоупа piece. Каркас+блокер по спеке НЕ понадобился: подписка на `add_message` доступна (см. verify).
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — exit 0, 0 errors (те же предсуществующие web-warning вне скоупа). Токен-чек: `git diff --cached | grep -c eyJ0` = 0. Юнит-тест не добавлял: новой чистой арифметики нет, `extractMessage` — тривиальный обход пути.
- Верификация: pass. Живой smoke на dev-стенде подтвердил доступность события ДО кода: `POST /api/v4/webhooks {destination, settings:['add_message']}` → **200**, ответ эхом `{"add_message":1}` (событие валидно для amo v4, id 48084282); `DELETE /webhooks {destination}` → **204**. Полный резюм-цикл (амо реально дёргает resumeUrl при входящем сообщении) в рантайме движка живьём не прогонялся: resumeUrl существует лишь внутри engine и требует подключённого чат-канала; формы вызовов и двухфазная логика выверены по коду, механика pause/resume идентична провалидированному T026. Скилл `verify` не запускался (нет доступного рантайма движка в итерации; e2e не обязателен — «verify: общий»). code-review (low) по диффу: находок нет (зеркало T026, чей паттерн уже провалидирован).
- Блокеры: нет. Ограничение (не блокер) в коде (ponytail): account-wide вебхуки + отсутствие ре-паузы → резюм по любому `add_message`. Вебхук-подписки smoke прибраны (DELETE 204).

### 2026-07-11 — T026: ожидание выполнения задачи (waitpoint)
- Статус: done (минимальный webhook-waitpoint по escape-hatch спеки, ограничение задокументировано ponytail)
- Изменения: `src/lib/actions/wait-for-task-completed.ts` (action `wait_for_task_completed`): props зеркалят `create_task` (text required, `taskTypeDropdown`, `entity_type`/`entity_id` через `taskEntityDropdown`, `userDropdown`, срок offset/`due_at` через `taskTime.computeCompleteTill` + дефолт now+1день). `run` двухфазный по `context.executionType`: **BEGIN** — `POST /tasks` (создаёт задачу как T015), `context.run.createWaitpoint({type:'WEBHOOK'})`, регистрирует amo-вебхук `POST /webhooks {destination: waitpoint.buildResumeUrl(), settings:['update_task']}`, кладёт `{taskId, resumeUrl}` в `context.store` (ключ `wait_for_task_completed:{run.id}:{step.name}`), `waitForWaitpoint(id)`, возвращает `{taskId, completed:false}`; **RESUME** — читает store, best-effort `DELETE /webhooks {destination}` (через `tryCatch(()=>…)`), `GET /tasks/{taskId}` (авторитетно), возвращает `{taskId, completed: is_completed===true, task}`. Разбор ответов через `unknown`+guards (`readTaskId`/`isTaskCompleted`/`isRecord`), без any/as; типы в конце файла. Регистрация в `actions/index.ts` (+waitForTaskCompleted, после completeTask); i18n +2 identity-ключа (displayName + description; props-ключи уже были от create-task).
- Решение (главное, по спеке «Спроектировать минимально; при сложности — ponytail: и блокер»): полный per-task резюм в одном action **недостижим** штатными примитивами — вебхуки amo account-wide (нет фильтра по task_id), срабатывают на ЛЮБОЙ `update_task` в аккаунте; verified платформенный паттерн (approval/webhook/assemblyai pieces) паузит один раз на BEGIN и не ре-паузит для фильтрации. Поэтому: резюм на первом `update_task`, а на RESUME повторно читаем ИМЕННО нашу задачу через `GET /tasks/{id}` → флаг `completed` отражает реальное состояние нашей задачи (а не случайной сработавшей). Подписка чистится best-effort на resume; flow, который не резюмится, оставляет вебхук в amo — потолок задокументирован `ponytail:`-комментом. Точный per-task резюм потребовал бы trigger/DP-side связки (вне скоупа piece).
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — exit 0, 0 errors (те же предсуществующие web-warning вне скоупа). Токен-чек: `git diff --cached | grep -c eyJ0` = 0. Юнит-тест не добавлял: арифметика срока покрыта существующим `task-time.test.ts` (T015), новой чистой логики нет.
- Верификация: pass. Живой smoke на dev-стенде подтвердил все зависимые формы ДО/во время кода: `POST /tasks` → создание (id 51962727), `GET /tasks/{id}` → `is_completed` **boolean** (false→ после `PATCH {is_completed:true}` →true), `POST /webhooks {destination, settings:['update_task']}` → **201**, `DELETE /webhooks {destination}` → **204**. Полный резюм-цикл (амо реально дёргает resumeUrl) в рантайме движка живьём не прогонялся — resumeUrl существует лишь внутри engine; формы всех вызовов и двухфазная логика по коду выверены. Скилл `verify` не запускался (нет доступного рантайма движка в итерации; e2e-шаг в спеке не обязателен — «verify: общий»). code-review (low) по диффу: 1 находка PLAUSIBLE/low — store-ключ был только по `run.id` (коллизия двух wait-шагов в одном run в цикле/параллели); **исправлено** дополнением `:{step.name}` к ключу, пере-lint+build зелёные.
- Блокеры: нет. Ограничение (не блокер) вынесено в код: account-wide вебхуки + отсутствие ре-паузы → резюм по любому `update_task`, компенсируется авторитетным `GET /tasks/{id}` на RESUME. Smoke-задача 51962727 (completed) осталась на тестовом аккаунте (безвредно).

### 2026-07-11 — T025: полировка дропдаунов + generic webhook действие
- Статус: done
- Аудит refresher'ов (правок кода не потребовал — всё уже корректно): `statusDropdown` `refreshers:['pipelineId']` совпадает с ключом `pipelineId` в lead-payload/copy-lead; `catalogElementDropdown` `refreshers:['catalog_id']` совпадает с `catalog_id` в link/unlink-catalog-element; `entity_id`-дропдауны через `taskEntityDropdown`/`linkedEntityDropdown` во всех потребителях T015/T017/T018/T023 (+ T020 run-salesbot) рефрешатся по `entity_type` (или `to_entity_type` для второй пары в link/unlink) — `entityByTypeDropdown` берёт `typeProp` в `refreshers` и читает `propsValue[typeProp]`; `entityTypeProperty`/StaticDropdown отдают ровно `leads`/`contacts`/`companies`, что совпадает с проверкой в дропдауне. Расхождений нет.
- Изменение (единственное): `src/index.ts` — `ponytail:`-коммент над `createCustomApiCallAction`, документирующий сознательный пропуск action `send_webhook` (по escape-hatch спеки: amoCRM-scoped custom api call + generic HTTP piece уже покрывают произвольные method/URL/headers/body; выделенный action заводить только под конкретную предзаготовленную форму вебхука).
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (те же предсуществующие web-warning вне скоупа). Токен-чек: токен-чек на утечку JWT (`git diff` по префиксу токена) = 0.
- Верификация: дифф — один коммент-блок, рантайм-поверхности нет (verify/skill не требуется); аудит refresher'ов выполнен чтением кода (grep по всем потребителям + сверка ключей). code-review излишен для one-comment диффа.
- Блокеры: нет. Решение: `send-webhook.ts` НЕ создавался — спека явно разрешает пропуск при покрытии custom api call + HTTP piece.

### 2026-07-11 — T024: copy / clone lead
- Статус: done
- Изменения: `src/lib/actions/copy-lead.ts` (action `copy_lead`: props `source_lead_id` (leadDropdown required), `new_name` (ShortText optional), `pipelineId`/`statusId` — переиспользованы `pipelineDropdown`/`statusDropdown` (ключи именно `pipelineId`/`statusId`, чтобы работал refresher статуса; displayName «Pipeline»/«Status» из фабрик — цель «target pipeline/status» покрыта), `copy_notes`/`copy_tasks` (Checkbox); `run`: `GET /leads/{id}?with=contacts,companies,catalog_elements` → резолв статуса → `POST /leads [body]` → `_embedded.leads[0]` → при флагах копирование notes/tasks на новую сделку). Pure `resolveTargetStatusId` (экспорт `copyLeadInternals` для теста): при заданном `targetStatusId` — он; иначе при той же воронке и не-unsorted статусе — статус источника; иначе первый нормальный статус целевой воронки (`type:0`, не 142/143, мин `sort`). unsorted = `type:1` («Неразобранное») — снято живьём со стенда. `buildCopyBody`: name (new_name→source→'Copy'), price/responsible/pipeline/status через `spreadIfDefined`, `custom_fields_values` переиспользуются из источника напрямую (уже в формате API), `_embedded.tags/contacts/companies` по id только при непустых. `copyNotes` — per-item `POST /leads/{new}/notes` best-effort (tryCatch, системные notes amo отвергает → скип; ponytail-коммент). `copyTasks` — `GET /tasks?filter[entity_type]=leads&filter[entity_id]={src}` → batch `POST /tasks` массивом (text/complete_till/task_type_id/responsible, entity на новую сделку). Колокейт `copy-lead.test.ts` (4 теста resolveTargetStatusId). Регистрация в `actions/index.ts` (+copyLead, 22 actions). i18n +5 identity-ключей (Copy Lead + описание, New Lead Name, Copy Notes, Copy Tasks; Pipeline/Status/Lead уже были). Без any/as (unknown + readNumber/readString/isRecord guards).
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx vitest run .../copy-lead.test.ts` — 4/4 passed; `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — exit 0, 0 errors (те же предсуществующие web-warning, вне скоупа). Токен-чек: токен-чек на утечку JWT (`git diff` по префиксу токена) = 0.
- Верификация: pass. E2E-replay полной логики `run()` на dev-стенде (источник lead 38653627): `GET with=` → резолв (source status 142, type 0 → сохранён) → `POST /leads` телом-объектом в массиве → **200**, новый lead 38735671; контрольный GET подтвердил status 142, pipeline 11074650, компания 43534287 перенесена, `custom_fields_values` присутствуют; копирование notes — 3/3 `common` note скопированы (`POST /leads/38735671/notes` → 2xx каждая); tasks источника — 0 (нечего копировать). code-review (low) по диффу — 2 находки, обе PLAUSIBLE и по-спеке-осознанные (см. решения), фикса не требуют.
- Решения: (1) `pipelineId`/`statusId` вместо `target_pipeline_id`/`target_status_id` из спеки — иначе refresher `statusDropdown` (`refreshers:['pipelineId']`) не сработал бы; семантика «целевые воронка/статус» сохранена, переиспользованы готовые дропдауны без дублей i18n. (2) unsorted-скип по `type:1`: сохраняем статус источника только если та же воронка И статус не unsorted; иначе первый нормальный (`type:0`, не 142/143, мин sort). Won/lost (142/143) при том же пайплайне СОХРАНЯЮТСЯ (спека предписывает скипать только unsorted — копия закрытой сделки ляжет в закрытый статус; code-review flag #1, оставлено по спеке). (3) notes/tasks — best-effort: сбой копии note/task не роняет action (tryCatch, ponytail-коммент; code-review flag #2 — осознанный молчаливый скип системных notes). (4) catalog_elements НЕ переносятся (спека требует переносить только contacts/companies; перелинковка элементов требует metadata — покрыто `link_catalog_element` T022). (5) tasks батчатся одним POST (однотипные, N+1 не нужен); notes per-item (смешанные типы, устойчивость к отказу системной note).
- Блокеры: нет. Наблюдение вне скоупа: `DELETE /api/v4/leads` и `/leads/{id}` на стенде → **405** (тот же scope private-токена, что у контактов/компаний в T013/T014) — smoke-копия lead 38735671 осталась на тестовом аккаунте (безвредно, удаление вне T024).

### 2026-07-11 — T023: change responsible с каскадом
- Статус: done
- Изменения: `src/lib/actions/change-responsible.ts` (action `change_responsible`: props `entity_type` StaticDropdown leads/contacts/companies, `entity_id` через `linkedEntityDropdown` (refresher entity_type), чекбокс `random_user` + опциональный `userDropdown` (спецрежим rand из спеки реализован чекбоксом — как предложено «проще»), 6 каскадных чекбоксов по спеке; `run`: PATCH основной сущности → при каскадных флагах один `GET /{entity}/{id}?with=<по типу>` → сбор id иммутабельно (flatMap/unique) → **batch-PATCH** массивом `[{id, responsible_user_id}]` по каждой коллекции чанками по 250 (N+1 избегнут, как требовала спека); открытые/закрытые сделки различаются догрузкой статусов `GET /leads?filter[id][]=` (пропускается, если выбраны оба флага); открытые задачи — `GET /tasks?filter[entity_type]=&filter[entity_id]=&filter[is_completed]=0` через fetchAllPages; random — `GET /users`, фильтр `rights.is_active===true`, ошибка если нет ни одного; если не задан ни user, ни random — читаемая ошибка). Трактовка `change_in_parent_entity` задокументирована в коде: lead → главный контакт (`is_main`), contact → его компания, company → нет родителя. Регистрация в `actions/index.ts` (21-й action), i18n +13 identity-ключей. aiMetadata с `idempotent:false` (random-режим не идемпотентен).
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — exit 0, 0 errors (те же 72 предсуществующих web-warning, вне скоупа). Токен-чек: `git diff HEAD | grep -c eyJ0` = 0 (и в новом файле 0).
- Верификация: pass. Живой smoke на dev-стенде ДО кода подтвердил все формы: `with=` (leads→contacts,companies; contacts→leads,companies; companies→leads,contacts; embedded contact несёт `is_main`), batch `PATCH /contacts|/tasks` массивом → 200, `GET /tasks?filter[entity_type]=leads&filter[entity_id]=&filter[is_completed]=0` (204 пусто / список), `GET /leads?filter[id][]=A&filter[id][]=B`, `rights.is_active` у `/users`. Затем e2e-replay полной каскадной последовательности из `run()` (lead 38653627 + company 43534287 + линкованный contact 43598475 + открытая задача): PATCH lead → GET with= → batch PATCH companies/contacts/tasks — все 200, контрольный GET каждой из 4 сущностей показал нового responsible 13354958. Сцена прибрана (responsible возвращены на 2898108, задача завершена, контакт отлинкован — unlink 204). code-review (low) по диффу — находок нет (`(none)`); оба lead-флага включены в условие `wantsLinks`, дубль `isRecord` — принятая идиома piece (V001).
- Решения: (1) все ветви каскада батчатся одним PATCH на коллекцию (чанк 250) — требование спеки против N+1 выполнено, ponytail-коммент не нужен; (2) выбор открытых/закрытых сделок: если включены оба флага — статусы не догружаются (все linked ids идут в PATCH); (3) закрытые = системные статусы 142/143 (константа с комментом).
- Блокеры: нет.

### 2026-07-11 — T021 BLOCKED + V004: чекпоинт-валидация блока T017–T021
- Статус: T021 → BLOCKED; V004 → done (фиксов кода не потребовалось).

**T021 (subscribe / unsubscribe user) — BLOCKED: у amoCRM v4 нет публичного endpoint подписчиков.**
- Живая разведка на dev-стенде (lead 38653627, user 2794642) до кода:
  - `GET /api/v4/leads/38653627/subscribers` → **404** (detail дропает `/api/v4` — эндпоинта нет); `POST` туда же → 404.
  - `GET /api/v4/leads/38653627/subscriptions` → **204** (пусто), но `POST` → **405** `allow: GET` — эндпоинт read-only; на `contacts`/`companies` его вовсе нет (404).
  - `PATCH /api/v4/leads/38653627 {_embedded:{subscribers:[{user_id}]}}` → **200**, но ключ молча проигнорирован (повторный GET /subscriptions по-прежнему 204) — записать подписчика через PATCH сущности нельзя.
  - Кандидаты `followers`/`subscription` → 404.
- Вывод: виджет Triggeron EntitySubscribe работает через внутренние AJAX-эндпоинты amo (сессионная авторизация + CSRF), недоступные публичной OAuth/long-lived-интеграции. Публичного write-механизма нет → по escape-hatch спеки и PROMPT.md задача помечена `BLOCKED` в prd.md, файлы-огрызки не создавались.

**V004 (чекпоинт T017–T021):**
- Полные проверки репо: `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (те же 72 предсуществующих web-warning, вне скоупа); `npm run test-unit` — `@activepieces/shared` 430/430, engine зелёный, **web#test падает: 8 тестов** (`output-table-view.test.ts` 3 + `utils-schema.test.ts` 5) — предсуществующий репо-долг (зафиксирован ещё в V001), `packages/web` ветка не трогает (`git diff main -- packages/web` пуст), чинить нельзя (вне разрешённых путей); локальный `npx vitest run` в piece — **4 файла / 25 тестов passed** (client/task-time/tags/custom-fields). `git status` — чисто (только prd.md/activity.md этой итерации).
- Структура: скоуп-проверка `git diff main --stat` — изменения только в `packages/pieces/community/amocrm/`, `ralph/`, `tsconfig.base.json` (T001) — чисто; grep `: any|as [A-Z]` по src без тестов — 0 совпадений; T021 не оставила огрызков (нет `subscri*` в src, не в index).
- Сверка со спеками (свежий sonnet-верификатор, независимый контекст): T017–T020 — **все СООТВЕТСТВУЮТ**, блокирующих расхождений нет. Подтверждено попунктно: T017 tags — гонка read-modify-write задокументирована `ponytail:` в tags.ts, mergeTags сохраняет чужие теги по id + дедуп новых по name; T018 link/unlink — POST /{entity}/{id}/link|unlink body-массив; T019 find — потолок «один filter-пара» задокументирован ponytail, 204→[]; T020 salesbot — run на /api/v2, stop не публикован (нет endpoint, escape-hatch). i18n displayName/description identity-mapped. T021 BLOCKED оформлен по PROMPT.md. Единственная заметка верификатора (не баг): T017 tags-prop — только Array строк, без отдельного tagDropdown-выбора существующих (осознанное упрощение, задокументировано в activity T017; merge/remove матчат id по String — функционально работает). code-review: каждая задача блока (T017–T020) прошла ревью в своей итерации + свежий верификатор пере-проверил корректность; скилл code-review по чистому рабочему дереву добавить нечего.
- Токен-чек: токен-чек на утечку JWT (`git diff` по префиксу токена) = 0.
- Фиксы: не потребовались (весь блок зелёный по спеке/паттернам). Коммит только документационный (prd.md чекбоксы + эта запись).
- Блокеры: T021 (см. выше) — единственный BLOCKED блока, оформлен корректно. Наблюдение вне скоупа: предсуществующие 8 красных web-тестов — репо-долг, не связан с amocrm.

### 2026-07-11 — T020: salesbot run / stop
- Статус: done (реализован `run_salesbot`; `stop_salesbot` не публикован — у amoCRM нет публичного endpoint, escape-hatch спеки)
- Изменения: `src/lib/actions/run-salesbot.ts` (`run_salesbot`: props `bot_id` Number required — GET-списка ботов у amo нет (см. ниже), `entity_type` StaticDropdown leads/contacts/companies, `entity_id` через `linkedEntityDropdown` refresher `entity_type`; `POST /api/v2/salesbot/run` телом-массивом `[{bot_id, entity_id, entity_type:<число>}]`, где entity_type маппится string→код (`contacts:1, leads:2, companies:3`) через локальную const `ENTITY_TYPE_CODE`; aiMetadata idempotent:false; без any/as); `src/lib/common/client.ts` (добавлен optional named-param `apiVersion?: 'v2'|'v4'` в `makeRequest`, default `'v4'` — salesbot живёт на `/api/v2`, все прежние вызовы неизменны); регистрация в `actions/index.ts` (+runSalesbot = 17 actions); i18n +4 identity-ключа (Run Salesbot + description, Salesbot ID + description; Entity Type/Entity уже были).
- Решения: (1) `stop_salesbot` НЕ реализован. Живая проверка dev-стенда показала: `POST /api/v2/salesbot/stop` при любых параметрах (fake bot, real lead, без bot_id) отдаёт legacy-envelope `{response:{error, error_code:"104"}}` (amo «метод не найден»), тогда как `run` отдаёт современный envelope (`_links`+`message`) — публичного stop-эндпоинта у amoCRM v2/v4 нет. Ломающийся экшен, который всегда возвращает error 104, нарушил бы self-hosting-правило («включено в UI, но молча сломано»), поэтому не публикован. Спека T020 явно это допускает («если стоп идёт иначе — реализовать доступное, блокер в activity»). (2) `bot_id` — Property.Number, т.к. list-эндпоинт ботов отсутствует (`GET /api/v4/salesbot` и `/api/v2/salesbot` → 404). (3) entity_type числовой в v2 API — подтверждён живьём (2=lead: run с entity_type:2 искал сущность как lead). (4) `apiVersion` вынесен в `makeRequest`, а не raw-httpClient в экшене — переиспользует tryCatch + читаемые ошибки, non-breaking optional named-param.
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npx vitest run .../client.test.ts` — 4/4 passed (default v4 сохранён, тест зелёный); `npm run lint-dev` — 0 errors (те же 72 предсуществующих web-warning, вне скоупа). Токен-чек: токен-чек на утечку JWT (`git diff` по префиксу токена) = 0.
- Верификация: pass. Живой smoke на dev-стенде (lead 36632537): `POST /api/v2/salesbot/run` `[{bot_id:123, entity_id:36632537, entity_type:2}]` → `{"message":"Bots not found: 123"}` (bot-level ошибка, entity_type:2 принят как lead — форма тела и endpoint подтверждены); `[{bot_id:1, entity_id:1, entity_type:2}]` → 403 `{"message":"Entity not found or no rights: 1"}`. `stop` — см. решение (1), 104 при всех вариантах. Дифф механический (один экшен по паттерну link-entities + optional param в client); code-review — ручная сверка по чек-листу (required props, entity_type→код, path/apiVersion, регистрация index+массив, i18n identity, no any/as), находок нет; client.test.ts зелёный подтверждает non-breaking apiVersion.
- Блокеры: нет по T020 (закрыт по условию спеки). Открытый вопрос вынесен: публичный endpoint остановки salesbot у amoCRM отсутствует (error_code 104) — если появится/найдётся приватный метод, добавить `stop_salesbot` отдельным экшеном.

### 2026-07-11 — T019: search / find entity
- Статус: done
- Изменения: `src/lib/actions/find-entity.ts` (`find_entity`: props `entity_type` StaticDropdown leads/contacts/companies (required), `query` ShortText (полнотекст amo `query=`), `filter_field`+`filter_value` (одна пара `filter[<field>]=<value>`), `sort` StaticDropdown updated_at/created_at + `order` asc/desc → `order[<field>]=<dir>`, `limit` Number default 50 c потолком `Math.min(limit,250)`; сборка query через `URLSearchParams`, `with=contacts,companies` только для leads (другие сущности отвергают чужие embeds — ponytail); `GET /{entity_type}?...` → `extractEntities` возвращает `_embedded[entity_type]` массивом или `[]` при 204/пустом теле; aiMetadata idempotent:true; локальный `isRecord`-guard, без any/as); регистрация в `actions/index.ts` (+findEntity, 16 actions); i18n +15 identity-ключей (Find Entity + описание, Search Query + описание, Filter Field + описание, Filter Value, Sort Field, Updated At, Created At, Sort Order, Descending, Ascending, Limit + описание — Entity Type/Lead/Contact/Company уже были).
- Решения: (1) один filter-пара без AND/OR (ponytail-коммент в файле) — сложные условия через несколько find + Router, как задано спекой; (2) фильтр по кастом-полям вне action — в description честно указано использовать `query`; (3) `with=contacts,companies` шлём только для leads — на contacts/companies этот embed невалиден; (4) отдельные find_lead/find_contact не плодил — один action с dropdown сущности (спека).
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (те же предсуществующие web/api/engine warning, вне скоупа). Токен-чек: токен-чек на утечку JWT (`git diff` по префиксу токена) = 0.
- Верификация: pass. Живой smoke на dev-стенде подтвердил форму запроса и ответа: `GET /leads?query=test&limit=3&with=contacts,companies` → 200; `GET /leads?filter[name]=zzznotexist&limit=1` → **204** (пустое тело — extractEntities вернёт `[]`, не падает); `GET /contacts?order[updated_at]=desc&limit=2` → 200; позитивный `GET /leads?order[updated_at]=desc&limit=2&with=contacts,companies` → `_embedded.leads` массив из 2 (извлечение по entity_type совпало). code-review (low)/ручная сверка диффа по чек-листу (required props, потолок limit, query-сборка/энкодинг брекетов через URLSearchParams, 204→[], регистрация index+массив, i18n identity, no any/as) — багов корректности нет.
- Блокеры: нет. Наблюдение вне скоупа: `ralph/ralph.sh` изменён в рабочем дереве (не итерацией) — не коммитил.

### 2026-07-11 — T018: link / unlink entities
- Статус: done
- Изменения: `src/lib/actions/link-entities.ts` (`link_entities`: props `entity_type`/`entity_id`/`to_entity_type`/`to_entity_id`, `POST /{entity_type}/{id}/link` body-массив `[{to_entity_id, to_entity_type}]`), `unlink-entities.ts` (`unlink_entities`: то же тело, `POST .../unlink`); регистрация в `actions/index.ts` (импорт + массив). В `common/props.ts` обобщил `taskEntityDropdown` во внутреннюю фабрику `entityByTypeDropdown({displayName, required, typeProp})` и экспортировал `linkedEntityDropdown` — оба `entity_id`/`to_entity_id` рефрешатся каждый по своему type-props (`entity_type`/`to_entity_type`); `taskEntityDropdown` теперь делегирует туда (поведение прежнее: refresher `entity_type`, displayName `Linked Entity`). entity_type — локальный StaticDropdown lead/contact/company в каждом action (разные displayName). i18n +9 ключей (2 displayName + 2 description + 5 prop-лейблов; `Entity` уже был). aiMetadata idempotent у обоих.
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (30/30). Юнит-теста нет: чистой логики action не содержит (спека теста не требует). Токен-чек: токен-чек на утечку JWT (`git diff` по префиксу токена) = 0.
- Верификация: pass. Живой smoke на dev-стенде (lead 36632537 + contact 43290948): `POST /leads/{id}/link` `[{to_entity_id, to_entity_type:contacts}]` → **200**, ответ `_embedded.links:[{to_entity_id, to_entity_type, metadata:null}]` — форма тела подтверждена; `POST /leads/{id}/unlink` тем же телом → **204**. Спека совпадает 1:1. code-review: дифф механический (пара action по паттерну T017 + безопасный рефактор дропдауна); обобщение `taskEntityDropdown` не меняет поведение существующих потребителей (add/remove/remove-all-tags, tasks) — refresher и displayName те же, подтверждено lint+build; находок нет.
- Блокеры: нет. Заметки: (1) `entityTypeProperty` (StaticDropdown lead/contact/company) продублирован в обоих action-файлах — осознанно (разные displayName, самодостаточные ~13 строк; вынос — кандидат для T025-полировки, не сейчас). (2) `to_entity_id`/`entity_id`-дропдауны уже рефрешатся по своим type-props — T025 в части link-рефрешеров закрыт этой задачей.

### 2026-07-11 — V003: чекпоинт-валидация блока T012–T016
- Статус: done (фиксов кода не потребовалось)
- Изменения: только `ralph/prd.md` (чекбокс V003) и эта запись.
- Команды: `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (те же 72 предсуществующих web-warning); `npm run test-unit` — engine/shared зелёные, красен только предсуществующий `web#test` (те же 8 тестов `output-table-view.test.ts`/`utils-schema.test.ts`, что зафиксированы в V001, ветка web не трогает); `npx vitest run packages/pieces/community/amocrm` — 19/19 passed (client 4, custom-fields 9, task-time 6); `git status` — чисто до начала итерации.
- Структура: 10 action-файлов (payload-хелперы `lead/contact/company-payload.ts` — не actions) все зарегистрированы в `actions/index.ts` и через `...amocrmActions` в `src/index.ts`; 34 `name:` у actions/triggers — дублей 0; grep `: any|as [A-Z]` вне тестов — 0. i18n: скриптовая сверка всех `displayName/description/placeholder` — «недостающими» оказались ровно 29 строк `aiMetadata.description` (10 actions + 19 triggers); проверено по kommo/airtable — aiMetadata в i18n референсные pieces НЕ кладут, покрытие user-facing строк полное.
- Скоуп: `git diff main --name-only` вне разрешённых путей — только `.agents/designs/amocrm-piece-backlog.md`, `.claude/settings.json`, `.claude/scheduled_tasks.lock`, `.gitignore` — те же файлы из chore-коммитов оркестратора, что уже разобраны в V001; piece-коммиты блока (f38a23d1, ea8a8b08, 995d2b59, 2b1c9ff0, 05f107a5) чистые.
- Сверка со спеками: sonnet-верификатор (свежий контекст) прошёл T012–T016 попунктно — **все пять соответствуют**, существенных расхождений нет; отдельно подтвердил: PATCH-safe omission через `spreadIfDefined`, `custom_fields_values` только при непустом, `computeCompleteTill` (dueAt приоритетнее, дефолт now+86400 задокументирован ponytail-комментом), note_type — ровно 4 значения спеки, `create_note_with_file` отложен ПО спеке (ponytail-коммент в `create-note.ts` + запись T016). Косметические заметки верификатора (без фикса): `task_id` в update/complete — Number, не dropdown (спека dropdown не требует); `text` в create_note required для всех 4 типов (условной required-механики у Property нет). code-review (low) по диффу блока: runtime-багов нет; 1 находка — пара `isRecord`+`firstCreated<Entity>` продублирована 5× в create-* файлах; то же семейство дублей, что V001 осознанно принял (не баг, вынос в общий модуль — рефакторинг вне чекпоинта, кандидат при росте piece) — решение оставлено в силе.
- Блокеры: нет.

### 2026-07-11 — T014: create/update company
- Статус: done
- Изменения: `src/lib/actions/company-payload.ts` (общий билдер `companyPayload = { optionalProps, buildBody }` — зеркало `contact-payload.ts` без first/last name: props `responsible_user_id`/`tags`/`custom_fields(entity:'companies')`; `buildBody` через `spreadIfDefined` не шлёт пустые (PATCH-safe), custom_fields через `fetchCustomFieldsMeta`+`buildCustomFieldsValues` из T005 только при непустом, `_embedded.tags:[{name}]`); `create-company.ts` (`create_company`, name required, POST `/companies` массивом, возврат `_embedded.companies[0]` с guard-фолбэком, aiMetadata idempotent:false); `update-company.ts` (`update_company`, `company_id` required через `companyDropdown` + name optional, PATCH `/companies/{id}` объектом); регистрация в `actions/index.ts` (`amocrmActions` += createCompany, updateCompany); i18n +5 identity-ключей (Create/Update Company + описания, Company Name — Tags/Responsible User/Company/Custom Fields уже были). Без `any`/`as` (unknown+isRecord guard).
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (те же 72 предсуществующих web-warning, вне скоупа).
- Верификация: pass. Live smoke на dev-стенде: `POST /api/v4/companies` массивом `[{name, _embedded.tags:[{name}]}]` → 200, GET по id подтвердил `name=Ralph T014 smoke` — форма payload подтверждена живым API (create-ответ минимальный, без эха name — как у lead/contact). code-review (low) скилл по диффу — находок нет (`[]`): механический мирроринг провалидированного T013-эталона. Токен-чек: токен-чек на утечку JWT (`git diff` по префиксу токена) = 0.
- Блокеры: нет. Наблюдение вне скоупа: удаление компаний на dev-стенде через `DELETE /api/v4/companies` и `/companies/{id}` отдаёт 405 (как и у контактов в T013 — иной route/scope у private-token); 1 smoke-компания id 43598485 осталась на тестовом аккаунте (безвредно, удаление вне T014). Коммит 995d2b59.

### 2026-07-11 — T013: create/update contact
- Статус: done
- Изменения: `src/lib/actions/contact-payload.ts` (общий билдер `contactPayload = { optionalProps, buildBody }` — зеркало `lead-payload.ts` без lead-специфики: props `first_name`/`last_name`/`responsible_user_id`/`tags`/`custom_fields(entity:'contacts')`; `buildBody` через `spreadIfDefined` не шлёт пустые (PATCH-safe), custom_fields через `fetchCustomFieldsMeta`+`buildCustomFieldsValues` из T005, ключ `custom_fields_values` только при непустом массиве, `_embedded.tags:[{name}]` из Array строк); `create-contact.ts` (`create_contact`, name required, POST `/contacts` массивом, возврат `_embedded.contacts[0]`, aiMetadata idempotent:false); `update-contact.ts` (`update_contact`, `contact_id` required через `contactDropdown` + name optional, PATCH `/contacts/{id}` объектом); регистрация в `actions/index.ts` (`amocrmActions` += createContact, updateContact); i18n +8 identity-ключей (Create/Update Contact + описания, Contact Name, First/Last Name — Tags/Responsible User/Custom Fields/Contact уже были). Телефон/email — через multitext в custom_fields (T005), отдельных props нет. Без `any`/`as` (unknown+isRecord guard).
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (те же 72 предсуществующих web-warning, вне скоупа).
- Верификация: pass. Live smoke на dev-стенде: `POST /api/v4/contacts` массивом `[{name,first_name,last_name,_embedded.tags}]` → 201, GET по id подтвердил `first_name=Ralph`, тег `t013-smoke` привязан (amo при create перекомпоновал `name` из first+last — поведение amo, не наше); `PATCH /api/v4/contacts/{id}` `{last_name}` → изменил только last_name (частичное обновление корректно). Форма payload подтверждена живым API. code-review: механический мирроринг провалидированного T012-эталона, ручная сверка диффа по чек-листу (required/optional props, PATCH-safe omission, custom_fields entity, tags, POST-массив/PATCH-объект, регистрация, i18n identity) — находок нет; отдельный скилл code-review излишен как для T007-зеркала. Токен-чек: токен-чек на утечку JWT (`git diff` по префиксу токена) = 0.
- Блокеры: нет. Наблюдение вне скоупа: удаление контактов на dev-стенде через `DELETE /api/v4/contacts` (и `/contacts/{id}`) отдаёт 405 — иной route/scope у private-token; 2 smoke-контакта с тегом `t013-smoke` остались на тестовом аккаунте (безвредно, удаление вне T013). Коммит ea8a8b08.

### 2026-07-11 — T012: create/update lead
- Статус: done
- Изменения: `src/lib/actions/lead-payload.ts` (общий модуль двух action'ов: `leadPayload = { optionalProps, buildBody }` — опциональные props (price/pipelineId/statusId/responsible_user_id/tags/contact_id/company_id/custom_fields) и иммутабельная сборка тела: `spreadIfDefined` для скаляров, `_embedded.tags[{name}]`/`contacts[{id}]`/`companies[{id}]` только при непустых, `custom_fields_values` через `customFieldsUtils.fetchCustomFieldsMeta`+`buildCustomFieldsValues` (T005), meta-запрос пропускается при пустом custom_fields); `create-lead.ts` (`POST /leads` телом-массивом, возврат `_embedded.leads[0]` с guard-фолбэком на весь ответ, aiMetadata `idempotent: false`); `update-lead.ts` (`lead_id` через `leadDropdown` required + опциональный `name`, `PATCH /leads/{id}` телом-объектом, незаполненные поля не шлются); `actions/index.ts` (`amocrmActions`); регистрация в `src/index.ts` (spread перед custom api call); i18n +7 ключей (92 всего). Пайплайн custom fields T005 сошёлся без правок (типы/`.props`/ключевание по `String(field.id)` совместимы с DynamicProperties out of the box).
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (те же 72 предсуществующих web-warning).
- Верификация: pass. Живой smoke на dev-стенде: `POST /api/v4/leads` телом `[{name, price, _embedded:{tags:[{name}]}}]` → 200, ответ `_embedded.leads[0]` = `{id: 38734915, request_id, _links}` — форма create-ответа и извлечение подтверждены; `PATCH /api/v4/leads/38734915` телом-объектом `{price, custom_fields_values:[{field_id: 788899, values:[{value}]}]}` → 200 `{id, updated_at}` — PATCH-семантика и формат custom_fields_values подтверждены. code-review (low) по диффу — находок нет (локальный `isRecord` — принятое в V001 дублирование). Токен-чек: `git diff --cached | grep -c eyJ0` = 0. Коммит f38a23d1.
- Блокеры: нет. Заметка для T013/T014: `lead-payload.ts` — эталон интеграции custom fields; contact/company повторяют паттерн со своими props (не обобщать преждевременно).

### 2026-07-11 — V002: чекпоинт-валидация блока T006–T011
- Статус: done (фиксов кода не потребовалось)
- Изменения: только `ralph/prd.md` (чекбокс V002) и эта запись.
- Команды: `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (exit 0); `npm run lint-dev` — 0 errors (те же 72 предсуществующих web-warning); `npm run test-unit` — engine/shared зелёные, **web#test падает: те же 8 предсуществующих тестов** (`output-table-view.test.ts` + `utils-schema.test.ts`), что зафиксированы в V001 — ветка `packages/web` не трогает, вне разрешённых путей; `git status` — чисто.
- Структура: 19 триггеров, дублей `name` нет; grep `: any|as [A-Z]` вне тестов — 0; регистрация симметрична (19 файлов = 19 импортов = 19 в `amocrmTriggers` = `triggers:` в `src/index.ts`); все user-facing строки триггеров есть в `i18n/translation.json` identity-ключами (сверено скриптом; `aiMetadata.description` в i18n не входит — сверено с паттерном kommo). Скоуп: дифф блока a79ca08d..HEAD — только `packages/pieces/community/amocrm/` и `ralph/`.
- Сверка со спеками: sonnet-верификатор (свежий контекст) прошёл T006–T011 попунктно — **все шесть соответствуют**; lifecycle симметричен (POST/DELETE по destination, без webhookId), payload-пути по справочнику (`leads.*`, `contacts.*`, `companies.*`, singular `task.*`, `note_*` per-entity, `message.add`), delete-триггеры с `fetchFullRecord: false`, sampleData/test()/aiMetadata у всех, фабрика T004 у всех кроме спек-санкционированного standalone note-added. code-review (low) по диффу блока — находок нет. Отмеченные верификатором не-блокеры: (1) дубль гвардов webhooks.ts↔note-added.ts — уже зафиксирован в T010 как осознанный; (2) `test()` note-added обобщён на выбранную entity вместо буквального `/leads` из спеки — улучшение, не баг; (3) `payloadPath: 'message.add'` T011 остаётся непроверенным живым payload (задокументировано в T011).
- Блокеры: нет.

### 2026-07-11 — T011: Триггер входящего сообщения (chats/talks)
- Статус: done
- Изменения: `src/lib/triggers/incoming-message.ts` (триггер `incoming_message` через фабрику `createAmoWebhookTrigger` T004: `events:['add_message']`, `payloadPath:'message.add'`, `entityType:'talks'`, `fetchFullRecord:false` — сообщения нельзя догрузить по id, возвращаем payload как есть; sampleData — форма amoJo-сообщения `{id(uuid),chat_id,talk_id,contact_id,entity_id,entity_type,text,type,origin,created_at}`; description/aiMetadata предупреждают про требование подключённого канала amoJo); регистрация в `triggers/index.ts` (импорт + массив, +1 = 19 триггеров); i18n +2 ключа (displayName/description, 85 всего).
- Живая сверка имён событий на dev-стенде (сняла допущение шапки `message`/`add_talk`): `POST /api/v4/webhooks` с `settings:[message,add_talk,update_talk,add_message,incoming_chat_message]` → **400** с `NotSupportedChoice` ровно на `settings.0` (`message`) и `settings.4` (`incoming_chat_message`); `add_talk`/`update_talk`/`add_message` ошибок не дали. Повторный `POST` только с тремя валидными → **201**, эхо `{add_talk:1,update_talk:1,add_message:1}`; `DELETE` по destination → **204**. Вывод: имя `message` из шапки НЕвалидно, для входящего сообщения корректно `add_message`. `GET /api/v4/talks?limit=5&order[updated_at]=desc` → **200** (top-level список talks существует, на стенде подключён Wazzup, 2 реальных talk) — дефолтный `test()` фабрики с `entityType:'talks'` отдаёт живые talks.
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (те же 72 предсуществующих web-warning, вне скоупа). Токен-чек: токен-чек на утечку JWT (`git diff` по префиксу токена) = 0.
- Верификация: pass. Имена событий вебхука проверены живьём (см. выше). `payloadPath:'message.add'` и форма sampleData — **best-guess, живым payload НЕ проверены**: для диспатча реального `add_message` нужно отправить сообщение через канал amoJo (сторона клиента), что вне возможностей стенда; спека T011 это допускает («реализация + отметка при успешном lint без live-проверки вебхука»). Дифф — один вызов фабрики + регистрация + i18n; отдельный code-review излишен для механического триггера на провалидированной фабрике.
- Блокеры: нет (задача закрыта по условию спеки). Открытый вопрос для V002/будущей живой проверки: точный `payloadPath` события `add_message` (кандидат `message.add`) и имена полей сообщения в payload — подтвердить, когда будет доступна отправка через подключённый канал. `test()` отдаёт talk-объекты (не message) — единственный живой источник chat-данных, форма отличается от sampleData/payload; осознанный компромисс (у сообщений нет list-эндпоинта).

### 2026-07-11 — T010: Триггер note_added (add на lead/contact/company)
- Статус: done
- Изменения: `src/lib/triggers/note-added.ts` — **standalone `createTrigger`** (не через фабрику T004), один триггер с prop `entity` (StaticDropdown lead/contact/company, default lead). `onEnable` подписывается на ОДНО событие `note_${entity}` (не на все три); `onDisable` — DELETE по destination. `run` читает примечание из payload по пути `${plural}.note` (`fetchFullRecord`-логика не нужна — note целиком в payload, `/notes` top-level нет). `test` — по выбранной сущности: `GET /${plural}?limit=1&order[updated_at]=desc` → id → `GET /${plural}/{id}/notes?limit=5` → `_embedded.notes`. sampleData — реальная форма note-объекта с dev-стенда. Локальные гварды `resolveEventEntries`/`extractEmbedded`/`readId`/`isRecord` (как в webhooks.ts, без any/as; типы/const `ENTITY_PLURAL` в конце файла). Регистрация в `triggers/index.ts` (импорт + массив, +1). i18n +4 ключа (Note Added / description / Entity / prop-description; Lead/Contact/Company уже были) → 83 всего.
- Решение (webhook lifecycle, моя зона): события и payload-путь зависят от выбранной `entity`, а фабрика T004 держит `events`/`payloadPath` статичными в замыкании. Расширять уже провалидированный общий компонент ради одного потребителя — scope creep; standalone-триггер переиспользует `amoClient`/`amocrmAuth` и не крупнее шоехорнинга. Спека допускала оба пути (в т.ч. фильтрацию в `run`); выбрал подписку на один event, т.к. раздельные события существуют (см. ниже) — фильтр по сущности в `run` не нужен.
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (те же 72 предсуществующих web-warning, вне скоупа).
- Верификация: pass. **Живой вебхук на dev-стенде** (снял открытый вопрос спеки об именах событий): `POST /api/v4/webhooks` с `settings:[note_lead,note_contact,note_company]` → **201**, эхо вернул `{note_lead:1,note_contact:1,note_company:1}` — **раздельные note-события по сущности существуют и валидны для amo v4** (единый `add_note` не понадобился). `DELETE` по destination → **204**. Форма note-объекта для sampleData снята с реального `POST/GET /leads/{id}/notes` (поля `id`/`entity_id`/`note_type`/`params.text`/`created_at`/... подтверждены). code-review (low) по файлу — находок нет. Токен-чек: токен-чек на утечку JWT (`git diff` по префиксу токена) = 0.
- Блокеры: нет. **Непроверено живьём:** payload-путь `${plural}.note` (`leads.note`/`contacts.note`/`companies.note`) — вебхук не диспатчился на публичный destination, путь взят из разведки Triggeron (best-guess, предписано спекой). Если реальный payload amo кладёт note иначе — `run` вернёт `[]`; правится одной строкой пути в T010 или при чекпоинте V002. Наблюдение вне скоупа: гварды `isRecord`/`extractEmbedded`/`readId` теперь продублированы в 5 файлах piece (webhooks/props/custom-fields/client/note-added) — кандидат на вынос в `common` при росте, не баг.

### 2026-07-11 — T008: Триггеры company (4 шт)
- Статус: done
- Изменения: 4 триггера через `createAmoWebhookTrigger` (T004) — `company-added.ts` (`add_company`, `companies.add`), `company-updated.ts` (`update_company`, `companies.update`), `company-responsible-changed.ts` (`responsible_company`, `companies.responsible`), `company-deleted.ts` (`delete_company`, `companies.delete`, `fetchFullRecord: false`, sampleData `{id}`); общий `company-sample.ts` (полный company-объект, переиспользуется 3 триггерами — как lead/contact-sample); регистрация в `triggers/index.ts` (импорт + массив `amocrmTriggers`, +4); i18n +8 ключей (displayName/description). entityType `companies`, у add/update/responsible — дефолтный `test()` фабрики. aiMetadata у каждого. Прямое зеркало T007, без новой логики.
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (72 предсуществующих warning в web, вне скоупа).
- Верификация: pass. Дифф — 4 идентичных вызова фабрики + sample + регистрация; ручная сверка (уникальность name, регистрация в index+массиве, i18n identity, payload-пути по справочнику `companies.add/update/responsible/delete`) — находок нет; отдельный code-review излишен для механического зеркала T007 (фабрика T004 провалидирована живым вебхуком в T006). Токен-чек: токен-чек на утечку JWT (`git diff` по префиксу токена) = 0.
- Блокеры: нет.

### 2026-07-11 — T009: Триггеры task (3 шт)
- Статус: done
- Изменения: 3 триггера через `createAmoWebhookTrigger` (T004) — `task-added.ts` (`add_task`, `task.add`), `task-updated-or-completed.ts` (`update_task`, `task.update`, description отсылает к флагу `is_completed`), `task-deleted.ts` (`delete_task`, `task.delete`, `fetchFullRecord: false`, sampleData `{id}`); общий `task-sample.ts` (task-объект по реальной форме dev-стенда: `entity_id`/`entity_type`/`is_completed`/`task_type_id`/`text`/`result`/`complete_till`); регистрация в `triggers/index.ts` (импорт + массив `amocrmTriggers`, +3); i18n +6 ключей (79 всего). entityType `tasks`, у add/update — дефолтный `test()` фабрики. aiMetadata у каждого. Зеркало T006–T008, без новой логики.
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (те же 72 предсуществующих web-warning, вне скоупа).
- Верификация: pass. Живой вебхук на dev-стенде (снял допущение шапки про имена событий task): `POST /api/v4/webhooks` с `settings:[add_task,update_task,delete_task]` → **201**, ответ эхом вернул все три как `{add_task:1,update_task:1,delete_task:1}` — **имена событий валидны для amo v4**. `DELETE` по destination → **204**. Ключ payload `task` (singular) — из справочника шапки (не проверялся живым payload, вебхук не диспатчился). sampleData построен по реальному `GET /api/v4/tasks` (форма подтверждена). Дифф — 3 идентичных вызова фабрики + sample + регистрация; ручная сверка (уникальность name, регистрация index+массив, i18n identity, payload-пути) — находок нет; отдельный code-review излишен для механического зеркала. Токен-чек: токен-чек на утечку JWT (`git diff` по префиксу токена) = 0.
- Блокеры: нет.

### 2026-07-11 — T022: catalogs list / link element / unlink element
- Статус: done
- Изменения: `src/lib/common/props.ts` (+`catalogDropdown` (GET `/catalogs` → `_embedded.catalogs` через `fetchAllPages`) и +`catalogElementDropdown` (refresher `catalog_id`, GET `/catalogs/{id}/elements?limit=250` → `_embedded.elements`, ponytail-коммент про потолок 250 + плейсхолдер «Select a catalog first.»)); `src/lib/actions/find-catalog-elements.ts` (`find_catalog_elements`: props `catalog_id` (catalogDropdown required) + `query` (ShortText optional); GET `/catalogs/{id}/elements?query=` через URLSearchParams, `extractElements` → `_embedded.elements` массивом или `[]` при 204; aiMetadata idempotent:true; локальный isRecord guard); `link-catalog-element.ts` (`link_catalog_element`: props `lead_id` (leadDropdown), `catalog_id`, `element_id` (catalogElementDropdown refresher catalog_id), `quantity` Number optional; `POST /leads/{id}/link` телом-массивом `[{to_entity_id:element_id, to_entity_type:'catalog_elements', metadata:{catalog_id, quantity?}}]`, quantity в metadata только если задан; idempotent:true); `unlink-catalog-element.ts` (`unlink_catalog_element`: те же lead/catalog/element, `POST /leads/{id}/unlink` телом `[{to_entity_id, to_entity_type:'catalog_elements', metadata:{catalog_id}}]`); регистрация в `actions/index.ts` (+3 = 20 actions); i18n +11 identity-ключей (Find/Link/Unlink Catalog Element + 3 описания, Catalog, Catalog Element, Quantity, «Select a catalog first.», «Full-text search across the catalog elements.» — Lead/Search Query/Limit уже были) → 177. Без any/as.
- Решения: (1) `element_id` — catalogElementDropdown с refresher `catalog_id` (первая страница 250, ponytail-потолок как у entity-дропдаунов), а не голый Number: единообразие с остальным piece; крупные каталоги ищутся через find_catalog_elements + ручной id. (2) `metadata.catalog_id` шлётся всегда для link И unlink — живьём подтверждено, что amo требует catalog_id в metadata для `catalog_elements` (иначе связь не идентифицируется). (3) `quantity` в metadata условно — только при заданном значении (PATCH-safe-подобно, не слать undefined).
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (те же 72 предсуществующих web-warning, вне скоупа). Юнит-теста нет: чистой логики action не содержит (спека теста не требует). Токен-чек: `git diff | grep -c eyJ0` = 0; grep `: any|as [A-Z]` в новых файлах — 0.
- Верификация: pass. Живой smoke на dev-стенде до кода подтвердил все формы: `GET /api/v4/catalogs` → 200 `_embedded.catalogs` (5+ каталогов); `GET /catalogs/8587/elements?limit=2` → 200 `_embedded.elements` (с `_links.next`, custom_fields_values у элементов); `GET /catalogs/8587/elements?query=zzznotexist` → **204** (пусто → extractElements вернёт `[]`); `POST /leads/38653627/link` `[{to_entity_id:1906553, to_entity_type:catalog_elements, metadata:{quantity:2, catalog_id:8587}}]` → **200**, эхо `_embedded.links:[{...metadata:{quantity,catalog_id}}]` — форма тела и metadata подтверждены; `POST /leads/38653627/unlink` `[{...metadata:{catalog_id}}]` → **204**. code-review: дифф механический (find зеркалит find-entity по query/204→[]; link/unlink зеркалят link-entities + metadata; два дропдауна по паттерну entityDropdown), ручная сверка по чек-листу (required props, refresher catalog_id, path/тело, 204→[], регистрация index+массив, i18n identity, no any/as) — находок нет.
- Блокеры: нет. Наблюдение: smoke-связь элемента 1906553 со сделкой 38653627 прибрана unlink'ом (204).

## Формат записи

```
### <дата ISO> — <ID задачи>: <название>
- Статус: done | blocked | partial
- Изменения: какие файлы созданы/изменены (кратко)
- Команды: что запускалось (сборка/тесты/lint)
- Верификация: результат verify (pass/fail + вывод по существу)
- Блокеры: что помешало / открытые вопросы / что вынесено дальше
```

## Лог

### 2026-07-11 — T017: tags add / remove / remove-all
- Статус: done
- Изменения: `src/lib/common/tags.ts` (`tagsUtils` — чистые `normalizeEntityTags`/`mergeTags`/`removeTags` + `entityTypeProperty` StaticDropdown leads/contacts/companies + HTTP-хелперы `fetchEntityTags` (GET сущности → `_embedded.tags`) / `patchTags` (PATCH `_embedded.tags`); read-modify-write, `unique` из pieces-framework, дедуп по name case-insensitive, существующие теги сохраняются по `id`, новые по `name`; ponytail-коммент про неатомарность RMW и отсутствие delta-endpoint у amo v4) + колокейт `tags.test.ts` (6 тестов на нормализацию/merge/remove); три тонких экшена `add-tags.ts`/`remove-tags.ts`/`remove-all-tags.ts` (props entity_type + `taskEntityDropdown` entity_id с refresher entity_type; add/remove ещё `tags` Array; remove-all без GET — сразу PATCH `tags:[]`; aiMetadata idempotent:true у всех трёх); регистрация в `actions/index.ts` (+3 = 13 actions), реэкспорт tags в `common/index.ts`; i18n +6 identity-ключей (Add/Remove/Remove All Tags + описания; Tags/Linked Entity Type/Linked Entity уже были). Без any/as.
- Решения: (1) убрал дженерик `Property.StaticDropdown<TagEntity>` — он ломал вывод required (propsValue давал `TagEntity | undefined`, build падал TS2322); тип выводится из литералов options, хелперы принимают `entityType: string` (используется только в шаблоне пути). (2) `tags`-prop — `Property.Array` строк (имена тегов); нормализация к `{name}`/`{id}` в хелперах; searchable-выбор существующих — upgrade при спросе. (3) три файла на action по правилу репо «один файл = один action», общая логика в `tagsUtils` (без action→action импортов).
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx vitest run .../tags.test.ts` — 6/6 passed; `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5) после фикса дженерика (см. решение 1); `npm run lint-dev` — exit 0, 0 errors (те же предсуществующие web-warning вне скоупа). Токен-чек: токен-чек на утечку JWT (`git diff` по префиксу токена) = 0.
- Верификация: pass. Живой интеграционный smoke на dev-стенде (lead 38653627), повторяющий логику экшенов: seed `[A,B]` → add C (существующие по id + `{name:C}`) → `[A,B,C]`; remove B (остальные по id) → `[A,C]`; remove-all (`tags:[]`) → `[]` — все PATCH 200, GET подтвердил каждый шаг. Форма `_embedded.tags` (GET отдаёт `[{id,name,color}]`, PATCH принимает `{name}`|`{id}`, пустой массив очищает) подтверждена живьём до кода. code-review (low) по диффу: 1 находка низкой важности — near-copy-paste `add-tags`/`remove-tags` (различие в mergeTags vs removeTags), совпадает с идиомой репо «один файл на action» + общая логика уже в tagsUtils; правок не требует, багов корректности нет.
- Блокеры: нет. Тестовый lead 38653627 оставлен с пустым набором тегов (smoke прибран).

### 2026-07-11 — T016: notes create common / system / с файлом
- Статус: done (файловый вариант отложен по escape-hatch спеки)
- Изменения: `src/lib/actions/create-note.ts` (`create_note`: props `entity_type` StaticDropdown leads/contacts/companies (переиспользован ключ i18n «Linked Entity Type» из create-task), `entity_id` через существующий `taskEntityDropdown` с refresher `entity_type`, `note_type` StaticDropdown common/service_message/call_in/call_out (default common), `text` LongText required; `POST /{entity_type}/{entity_id}/notes` телом-массивом `[{note_type, params:{text}}]`, возврат `_embedded.notes[0]` с guard-фолбэком; aiMetadata idempotent:false; локальный `isRecord`-guard, без any/as); регистрация в `actions/index.ts` (+createNote, 10 actions); i18n +8 identity-ключей (Create Note + описание, Note Type, Note Text, Common/Service Message/Incoming Call/Outgoing Call — Linked Entity Type/Linked Entity/Lead/Contact/Company уже были).
- Решение: `create_note_with_file` НЕ реализован — спека явно разрешает («Если Files API сложен/недоступен — реализовать common/system сейчас, файл вынести подпунктом»). Причина: amo Files API v4 — многошаговая сессионная загрузка на отдельный drive-хост (create session → PUT parts → file_uuid → note типа attachment), тяжело и непроверяемо на стенде за итерацию. Оставлен `ponytail:`-коммент в create-note.ts с upgrade-путём. Для note_type call_in/call_out передаём `params:{text}` (минимум по спеке); полная модель call-полей (phone/duration/...) — вне скоупа.
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (те же предсуществующие web-warning, вне скоупа). Токен-чек: `git diff --cached | grep -c eyJ0` = 0.
- Верификация: pass. Живой smoke на dev-стенде перед кодом подтвердил форму payload: `POST /api/v4/leads/38653627/notes` телом `[{note_type:"common", params:{text:"Ralph T016 smoke"}}]` → 200, ответ `_embedded.notes[0]` (id 281146417) — форма и извлечение совпали с реализацией. Дифф механический (зеркалит create-task: entity_type + taskEntityDropdown + POST-массив + firstCreated*); ручная сверка по чек-листу (required props, refresher entity_type, path, body-массив, регистрация index+массив, i18n identity, no any/as) — находок нет; отдельный скилл code-review излишен.
- Блокеры: нет. Открытый вопрос вынесен в код (ponytail): `create_note_with_file` через Files API — когда будет доступен/проскоуплен scope files. 1 smoke-примечание id 281146417 осталось на тестовом аккаунте (безвредно). Наблюдение вне скоупа: `taskEntityDropdown` (displayName «Linked Entity») переиспользуется как parent-entity дропдаун note — семантика подходит, дублировать не стал.

### 2026-07-11 — T015: tasks create / update / complete
- Статус: done
- Изменения: 3 экшена — `create-task.ts` (props: text required, `taskTypeDropdown`, `entity_type` StaticDropdown leads/contacts/companies, `entity_id` via новый `taskEntityDropdown` с refresher `entity_type`, `userDropdown`, срок `due_offset_value`+`due_offset_unit` minutes/hours/days ИЛИ `due_at` DateTime; `POST /tasks` массив, ответ `_embedded.tasks[0]`), `update-task.ts` (`task_id` Number + опциональные text/срок/responsible, `PATCH /tasks/{id}` объект, `complete_till` шлётся только если задан — частичное обновление), `complete-task.ts` (`task_id`+`result_text`, `PATCH {is_completed:true, result:{text}}`); общий чистый хелпер `common/task-time.ts` (`taskTime.computeCompleteTill` → unix секунды, приоритет dueAt над offset, фолбэк offset при невалидном dueAt, `undefined` если ничего не задано — дефолт now+1день применяет create) + колокейт `task-time.test.ts` (6 тестов); `taskEntityDropdown` + рефактор общего `fetchEntityOptions` в `props.ts` (переиспользуется entityDropdown, без дублирования); регистрация в `actions/index.ts` (+3), реэкспорт task-time в `common/index.ts`; i18n +21 ключ (identity). aiMetadata у всех трёх, `idempotent:false` у create.
- Решения: (1) приоритет `due_at` над относительным offset (по спеке «Если задан dueAt — ... Иначе offset»); (2) дефолт complete_till при отсутствии срока — now+1 день (ponytail-коммент в create-task), amo требует complete_till; (3) `task_id` в update/complete — `Property.Number` (у задач нет name → dropdown бесполезен, спека требует лишь task_id); (4) `offsetUnit` в хелпере типизирован `string` + type guard `isOffsetUnit` — StaticDropdown отдаёт string, без `as`-каста.
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx vitest run .../task-time.test.ts` — 6/6 passed; `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5) после фикса двух пропущенных экспортов (task-time в common/index; сужение типа offsetUnit); `npm run lint-dev` — exit 0, 0 errors (те же предсуществующие web/api warning, вне скоупа).
- Верификация: pass. Живой smoke на dev-стенде подтвердил форму payload перед кодом: `POST /api/v4/tasks` массивом `[{text,complete_till(секунды),entity_type:"leads",entity_id}]` → **201**, ответ `_embedded.tasks[0]` (id 51962153); `PATCH /tasks/{id}` объектом `{text}` → 200 (вернул task-объект, не массив); complete `PATCH {is_completed:true,result:{text}}` → 200. Все три формы совпали с реализацией. code-review (low) по диффу — находок нет (дифф механический, повторяет паттерн create/update lead T012). Токен-чек: токен-чек на утечку JWT (`git diff` по префиксу токена) = 0.
- Блокеры: нет.

### 2026-07-11 — T007: Триггеры contact (4 шт)
- Статус: done
- Изменения: 4 триггера через `createAmoWebhookTrigger` (T004) — `contact-added.ts` (`add_contact`, `contacts.add`), `contact-updated.ts` (`update_contact`, `contacts.update`), `contact-responsible-changed.ts` (`responsible_contact`, `contacts.responsible`), `contact-deleted.ts` (`delete_contact`, `contacts.delete`, `fetchFullRecord: false`, sampleData `{id}`); общий `contact-sample.ts` (полный contact-объект, переиспользуется 3 триггерами — как lead-sample в T006); регистрация в `triggers/index.ts` (импорт + массив `amocrmTriggers`); i18n +8 ключей (displayName/description). entityType `contacts`, у add/update/responsible — дефолтный `test()` фабрики. aiMetadata у каждого. Зеркало T006, без новой логики.
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (72 предсуществующих warning в web, вне скоупа).
- Верификация: pass. Дифф — 4 идентичных вызова фабрики + sample + регистрация; ручная сверка (уникальность name, регистрация в index+массиве, i18n identity, payload-пути по справочнику) — находок нет; отдельный code-review излишен для механического зеркала T006 (фабрика T004 уже провалидирована живым вебхуком в T006). Токен-чек: токен-чек на утечку JWT (`git diff` по префиксу токена) = 0.
- Блокеры: нет.

### 2026-07-11 — T006: Триггеры lead (6 шт)
- Статус: done
- Изменения: 6 файлов через фабрику `createAmoWebhookTrigger` (T004) — `src/lib/triggers/lead-added.ts` (`add_lead`, `leads.add`), `lead-updated.ts` (`update_lead`, `leads.update`), `lead-status-changed.ts` (`status_lead`, `leads.status`), `lead-responsible-changed.ts` (`responsible_lead`, `leads.responsible`), `lead-deleted.ts` (`delete_lead`, `leads.delete`, `fetchFullRecord: false` — сущность удалена, sampleData `{id}`), `lead-restored.ts` (`restore_lead`, `leads.restore`); общий `lead-sample.ts` (полный lead-объект, переиспользуется 5 триггерами — ponytail: без 6 копий); `triggers/index.ts` (`amocrmTriggers`); `src/index.ts` (импорт + `triggers: amocrmTriggers`); i18n +12 ключей (displayName/description каждого). entityType `leads`, у всех кроме delete — дефолтный `test()` фабрики (`GET /leads?limit=5&order[updated_at]=desc`). aiMetadata у каждого. Один файл = один триггер, имена уникальны.
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — pass (30/30, 0 errors).
- Верификация: pass. Smoke на dev-стенде (первый живой вебхук — снял допущения T004/шапки): `POST /api/v4/webhooks` с `settings:[add_lead,update_lead,status_lead,responsible_lead,delete_lead,restore_lead]` → **201**, ответ эхом вернул все 6 событий как `{<event>:1}` — **все имена событий валидны для amo v4** (спека подтверждена). `DELETE /api/v4/webhooks` по `{destination}` → **204** — lifecycle enable/disable симметричен, удаление по destination работает (webhookId не нужен, как и решено в T004). Допущение о нескольких вебхуках с разным destination: amo хранит settings per-destination (подтверждено формой ответа), конфликта между flow-триггерами нет. code-review: дифф — 6 идентичных вызовов фабрики + данные, ручная сверка (уникальность name, регистрация в index+массиве, i18n identity) — находок нет.
- Блокеры: нет.

### 2026-07-10 — T001: Scaffold piece + auth + validate
- Статус: done
- Изменения: `packages/pieces/community/amocrm/` — `package.json`, `tsconfig.json`, `tsconfig.lib.json`, `.eslintrc.json`, `README.md`, `src/index.ts` (createPiece + createCustomApiCallAction, ponytail-коммент про логотип-заглушку), `src/lib/auth.ts` (CustomAuth: subdomain/zone/apiToken + validate через GET /account), `src/i18n/translation.json`. Плюс path `@activepieces/piece-amocrm` в `tsconfig.base.json` и механический `bun.lock`. Каркас был частично создан оборвавшейся прошлой итерацией (без коммита и записи) — эта итерация доверила, доверификовала и закоммитила. `project.json` не создавался: у kommo его нет, репо на turbo.
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5 tasks); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (72 предсуществующих warning в web, вне скоупа); smoke на dev-стенде: `GET /api/v4/account` c валидным токеном → 200, с невалидным → 401 (validate отработает верно).
- Верификация: pass. code-review (low) по диффу — находок нет.
- Блокеры: нет. Замечания: (1) `ralph/ralph.sh` изменён в рабочем дереве (добавлен `--model fable`) — вне разрешённых путей итерации, оставлен незакоммиченным (позже закоммичен параллельно человеком/оркестратором как c34c7e0e); (2) `tryCatch` реэкспортируется из `@activepieces/pieces-framework` — в piece импортируем оттуда, не из `@activepieces/shared` (import boundary); (3) **commitlint запрещает заглавные в subject** — `feat(amocrm): T001 ...` отклоняется, использовать строчное `t001`.

### 2026-07-10 — T002: common/client.ts — makeRequest + пагинация
- Статус: done
- Изменения: `src/lib/common/client.ts` (`amoClient = { makeRequest, fetchAllPages }`: named params, URL из subdomain/zone, Bearer, tryCatch, читаемые ошибки из тела amo; пагинация page++ с остановкой по отсутствию `_links.next`/пустому `_embedded`, жёсткий предел 100 страниц), `src/lib/common/index.ts` (реэкспорт), колокейт `client.test.ts` (4 теста: URL/заголовки, проброс ошибки amo, склейка 2 страниц + остановка, пустой 204-ответ).
- Решение: спека требует `ActivepiecesError`, но он живёт в `@activepieces/shared`, запрещённом для pieces (import boundary), и НЕ реэкспортируется из `pieces-framework` — бросаю обычный `Error` с `amoCRM API error (<status>): <body>` (HttpError из pieces-common даёт status/body). В тесте есть `as HttpRequest` на mock.calls — это принятый паттерн репо (coupa client.test.ts), тип теряется в vi.spyOn.
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx vitest run .../client.test.ts` — 4/4 passed; `npx turbo run build --filter=@activepieces/piece-amocrm` — pass; `npm run lint-dev` — 0 errors (те же 72 предсуществующих warning в web). Корневой `npm run test-unit` piece-тесты не подхватывает (script фильтрует engine/shared/web) — по спеке достаточно локального vitest.
- Верификация: pass. code-review (low) — находок нет. Smoke на dev-стенде: `GET /api/v4/leads?page=1&limit=1` → ключи `_embedded.leads`, `_links.next` — форма совпадает с реализацией fetchAllPages.
- Блокеры: нет.

### 2026-07-10 — T004: Фабрика webhook-триггеров + lifecycle
- Статус: done
- Изменения: `src/lib/common/webhooks.ts` (`createAmoWebhookTrigger` — named-params фабрика, `TriggerStrategy.WEBHOOK`: `onEnable` POST /webhooks `{destination, settings: events}`, `onDisable` DELETE /webhooks `{destination}` без хранения webhookId, `run` идёт по `payloadPath` (split '.'), маппит ВСЕ записи события, при `fetchFullRecord`(default true) GET `/{entityType}/{id}?with=contacts,companies,catalog_elements` иначе возвращает payload как есть, `test` параметризуем через `testFn` иначе дефолт GET `/{entityType}?limit=5&order[updated_at]=desc`, опциональные `props`); `src/lib/common/index.ts` (+реэкспорт webhooks). Разбор payload через `unknown` + гварды (`isRecord`/`resolveEventEntries`/`readId`), без any/as; типы в конце файла.
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (те же 72 предсуществующих warning в web, вне скоупа).
- Верификация: pass. code-review (low) по файлу — находок нет. Типизация `context.auth.props` в trigger-контексте прошла tsc — auth-распаковка через `.props` корректна и для триггеров (как в props.ts). Фабрика без потребителей — живой вебхук проверится в T006.
- Блокеры: нет. Заметки: (1) `onDisable` шлёт DELETE без guard — если вебхук уже удалён, amo может вернуть 404 и makeRequest бросит; поведение предписано спекой (удаление по destination), проверить на живом вебхуке в T006. (2) `run` использует `Promise.all` GET'ов — для не-delete событий сущность существует; delete-события идут с `fetchFullRecord: false`, GET не выполняется.

### 2026-07-10 — T005: динамические props кастомных полей
- Статус: done
- Изменения: `src/lib/common/custom-fields.ts` (`customFieldsUtils = { customFieldsProperty, fetchCustomFieldsMeta, buildCustomFieldsValues }`: DynamicProperties с ключеванием `String(field.id)`, маппинг amo-типов → Property (textarea→LongText, numeric/monetary→Number, select/radiobutton→StaticDropdown enum_id, multiselect→StaticMultiSelectDropdown, checkbox→Checkbox, date/date_time/birthday→DateTime, text/url/multitext/неизвестные→ShortText); `buildCustomFieldsValues` — чистая, скип пустых/неизвестных id, NaN-даты и NaN-enum_id отбрасываются, multitext берёт `enum_code` из `enums[0].value` c фолбэком `'WORK'`), колокейт `custom-fields.test.ts` (9 тестов), реэкспорт в `common/index.ts`, ключ `Custom Fields` в i18n.
- Решения сверх спеки (подтверждены живым API): (1) на dev-стенде есть тип `date_time` (не упомянут в спеке) — добавлен в date-группу (unix-секунды); (2) тип `tracking_data` (19 utm-полей на leads) — падает в ShortText-фолбэк, как и любые будущие неизвестные типы; (3) числовое значение date-поля трактуется как уже готовые unix-секунды (ISO — из DateTime prop). Живые формы: `/leads/custom_fields` и `/contacts/custom_fields` → `_embedded.custom_fields`, у multitext коды лежат в `enums[].value` (`WORK`/`MOB`/...) — спека подтверждена.
- Команды: `npx vitest run .../custom-fields.test.ts` — 9/9 passed; `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (те же 72 предсуществующих warning в web).
- Верификация: pass. code-review (low) — находок нет. Коммит 1f760572.
- Блокеры: нет. Заметка для T012: в `run()` экшена — `fetchCustomFieldsMeta({ auth: context.auth.props, entity })` → `buildCustomFieldsValues({ fieldsMeta, values: propsValue.custom_fields ?? {} })`, ключ `custom_fields_values` добавлять только при непустом массиве.

### 2026-07-11 — V001: чекпоинт-валидация блока T001–T005
- Статус: done (фиксов кода не потребовалось)
- Изменения: только `ralph/prd.md` (чекбокс V001) и эта запись.
- Команды: `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (72 предсуществующих warning в web); `npm run test-unit` — engine/shared зелёные, **web#test падает: 8 тестов** в `output-table-view.test.ts` (3) и `utils-schema.test.ts` (5) — предсуществующий фейл, `git diff main -- packages/web` пуст (ветка web не трогает), чинить нельзя (вне разрешённых путей); `git status` — чисто.
- Структура: в `src/lib/` только `auth.ts` + `common/` (actions/triggers ещё нет — регистрация тривиально консистентна); grep `: any|as [A-Z]` вне тестов — 0 совпадений; все displayName/description/placeholder из исходников присутствуют в `i18n/translation.json` (сверено списками).
- Скоуп: piece-коммиты (t001–t005) чистые; файлы вне разрешённого списка в диффе против main (`.agents/designs/`, `.claude/settings.json`, `.claude/scheduled_tasks.lock`, `.gitignore`) — из chore(ralph)-коммитов оркестратора 792b25c3/c34c7e0e, не из итераций; `bun.lock` — механический результат t001.
- Сверка со спеками: sonnet-верификатор (свежий контекст) прошёл T001–T005 попунктно — **все пять соответствуют**, расхождений нет; отдельно подтвердил, что flat `auth.subdomain` в `validate` (без `.props`) корректен для validate-callback (сверено с framework и coupa). code-review (low) по диффу блока: runtime-багов нет; 1 заметка — `extractEmbedded`/`isRecord` продублированы в webhooks.ts/props.ts/custom-fields.ts/client.ts; осознанно оставлено (не баг, вынос в общий модуль — рефакторинг вне чекпоинта, кандидат при росте piece).
- Блокеры: нет. Наблюдение вне скоупа: предсуществующие 8 красных web-тестов (см. выше) — репо-долг, не связан с amocrm.

### 2026-07-10 — T003: common/props.ts — дропдауны с пагинацией
- Статус: done
- Изменения: `src/lib/common/props.ts` (фабрики `pipelineDropdown`/`statusDropdown` (refresher pipelineId)/`userDropdown`/`taskTypeDropdown` (GET /account?with=task_types)/`tagDropdown({entity})`/`lossReasonDropdown` (required: false, без параметров)/`leadDropdown`/`contactDropdown`/`companyDropdown` — последние три через общий `entityDropdown`, одна страница `limit=250&order[updated_at]=desc`, label `name (id)`, ponytail-коммент про потолок 250; справочники — через `fetchAllPages`; разбор ответов через unknown + гварды `toOptions`/`extractEmbedded`, без any/as), `src/lib/common/index.ts` (реэкспорт props), `src/i18n/translation.json` (+11 ключей: displayName'ы и плейсхолдеры).
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npm run lint-dev` — 0 errors (72 предсуществующих warning в web). Smoke на dev-стенде: `/account?with=task_types` → `_embedded.task_types` массив (форма подтверждена); `/leads/tags` пагинирован (`_links.next`); `/leads/pipelines?page&limit`, `/leads/pipelines/{id}/statuses?page&limit`, `/users?page&limit`, `/leads?limit=250&order[updated_at]=desc` — все 200 (эндпоинты терпят параметры fetchAllPages/entity-дропдаунов).
- Верификация: pass. code-review (low) по диффу — находок нет.
- Блокеры: нет. Заметка: тип значения всех дропдаунов — number (id amo); `tagDropdown` — одиночный Dropdown, T017 при необходимости обернёт/расширит под множественный выбор.


### 2026-07-11 — T028: финальный прогон — i18n, lint-sweep, sampleData, README
- Статус: done
- Изменения: только `packages/pieces/community/amocrm/README.md` — заменён scaffold-заглушка (3 строки) на полноценный README (auth long-lived token + где взять, список всех триггеров и экшенов, упоминание custom fields, build). Код piece не менялся — аудит показал, что всё уже консистентно.
- Аудит (скриптовые проверки, факты):
  - **Регистрация actions:** 24 файла экшенов (excl helpers/tests/index) ↔ 24 импорта в `actions/index.ts` ↔ 24 записи в массиве `amocrmActions` — совпадают 1:1.
  - **Регистрация triggers:** 19 файлов триггеров (excl `-sample`/index) ↔ 19 импортов в `triggers/index.ts` — совпадают.
  - **Дубли name:** `grep -oE "name: '..'" | sort | uniq -d` по actions+triggers — пусто (дублей нет).
  - **sampleData:** все 19 триггеров содержат `sampleData` (по одному на файл) — 19/19 OK.
  - **i18n identity:** Python-скан всех `displayName`/`description`/`placeholder` строк исходников (с вырезанным блоком `aiMetadata`) против 197 ключей `translation.json` — 0 UI-facing missing. Все ключи identity-mapped (key==value, non-identity = []). Замечание: `aiMetadata.description` (43 длинных строки) НЕ в i18n — это корректно: aiMetadata AI-facing, не UI-переводимая (паттерн kommo/airtable), спека i18n-правила targets только displayName/description.
- Команды: `npx turbo run lint build --filter=@activepieces/piece-amocrm` — pass (6/6 tasks, lint+build); `npm run lint-dev` — exit 0, 30/30 tasks, 0 errors. Токен-чек: токен-чек на утечку JWT (`git diff` по префиксу токена) = 0. `git status` — только README.md изменён.
- Верификация: pass. Изменение docs-only (README), рантайм-поверхности нет — verify-скилл неприменим. Аудит зелёный по всем 5 пунктам спеки T028.
- Блокеры: нет. План закрыт: все T-задачи `[x]`, единственная незакрытая — T021 (BLOCKED: нет публичного endpoint подписчиков в amo v4, зафиксировано в V004).

<promise>COMPLETE</promise>

### 2026-07-11 — Фаза 6 открыта: Events API (poll + doorbell)
- Статус: план дополнен (оркестратор, вне итераций)
- Изменения: `ralph/prd.md` — блок T029–T039 + V006/V007 (коммит c1db0c77): клиент `/api/v4/events` с курсорной механикой, poll-фабрика, doorbell-фабрика (вебхук-будильник → дельта из ленты), триггеры per-field/бюджет/вход-в-статус/звонки/теги/универсальный, action find_events, spike ре-паузы (T038) и условный апгрейд wait-экшенов (T039). Справочник Events API добавлен в prd.md.
- Блокеры: нет. План снова содержит открытые задачи — цикл продолжается с T029.

### 2026-07-11 — T031: Doorbell-фабрика createAmoDoorbellTrigger + живая проверка покрытия
- Статус: done
- Изменения: `src/lib/common/events-doorbell.ts` (`createAmoDoorbellTrigger` — named-params фабрика, `TriggerStrategy.WEBHOOK`, паттерн «notification + delta pull»: `onEnable` POST /webhooks `{destination, settings: webhookEvents}` (реюз механики T004) + `store.put('cursor', {lastCreatedAt: now, lastIds: []})`; `onDisable` DELETE /webhooks `{destination}` + `store.delete('cursor')`; `run` игнорирует payload вебхука как данные, читает курсор (нет → инициализирует now, `[]`), `fetchEvents({from: cursor.lastCreatedAt, types: eventTypes/eventTypesFromProps, entity})` → `advanceCursor` → при пустом `fresh` возвращает `[]` и курсор НЕ двигает (лента отстаёт — доберётся следующим звонком), иначе store next + опц. `filterEvent(event, propsValue)`; `test` — последние события за неделю по типам, как в T030); реэкспорт в `common/index.ts`. Поддержаны `eventTypes | eventTypesFromProps` (для T032 per-field) и `filterEvent` (для T033 lead_entered_stage). entity_id-сужение fetchEvents из payload — опционально по спеке, не делал (ponytail: types-фильтр уже сужает, лишний парсинг payload не нужен). Новый тест не добавлял — `run()` не содержит новой чистой арифметики, курсор покрыт тестом T029 (спека это разрешает явно).
- Команды: `npx turbo run lint build --filter=@activepieces/piece-amocrm` — pass (6/6); `npm run lint-dev` — 30/30, 0 errors (те же предсуществующие web-warning вне скоупа). Токен-чек: `git diff --cached | grep -c eyJ0` = 0. `git status` — только 2 разрешённых файла (events-doorbell.ts + index.ts).
- ЖИВОЙ SMOKE ПОКРЫТИЯ (dzenteamdev, сделка 36632537, приёмник webhook.site) — результаты для T032/T033/T036:
  - Подписал вебхук `POST /webhooks {settings:[update_lead,status_lead]}` → 201 (id 48084570). Затем 3 мутации сделки через PATCH /leads/36632537: (1) price 75000→76000; (2) custom field 829516 (numeric «Кол-во Гостей») = 42; (3) добавил тег `doorbell-smoke`. Все PATCH → 200.
  - **Вебхук `update_lead` пришёл на ВСЕ 3 мутации** (webhook.site: 3 вызова, каждый `leads[update][0][...]` со `status_id/old_status_id/price`). Вывод: **doorbell-паттерн покрывает бюджет, кастом-поля И теги** — T032/T033/T036 могут строиться на doorbell (`update_lead` как будильник), poll-фолбэк не требуется для этих мутаций.
  - **Лента `/events` проиндексировала все 3 события за секунды** (задержка <2с): вебхуки на webhook.site в 09:23:24-26, события с `created_at` 1783761803/805/806. Задержка ленты пренебрежимо мала; логика «пустой fresh → курсор не двигать» страхует от редкого отставания.
  - **Формат событий (для sampleData T032/T033/T036):** `id` — ULID-строка (`01kx87t...`), `created_at` — unix-секунды, порядок ответа **desc по created_at** (свежее первым). Типы и `value_after`:
    - `sale_field_changed` → `value_after: [{sale_field_value: {sale: 76000, sale_with_minor_units: 76000.0}}]`
    - `custom_field_829516_value_changed` → `value_after: [{custom_field_value: {field_id: 829516, field_type: 2, enum_id: null, text: "42", is_masked: false}}]` (per-field тип = `custom_field_{ID}_value_changed`, подтверждён живьём)
    - `entity_tag_added` → `value_after: [{tag: {name: "doorbell-smoke"}}]`
  - Форма `filter[entity][]=lead&filter[entity_id][]=36632537&filter[created_at][from]=...` работает (сузила до 3 событий сделки). `filter[created_at][from]` включителен (события с created_at == from вернулись) — совпадает с дедупом lastIds в advanceCursor (T029).
  - Очистка стенда: `DELETE /webhooks` по destination → 204; тег снят PATCH → 200.
- Верификация: pass. Фабрика без потребителя (первый триггер — T032); lifecycle подписки/дедуп проверены живьём выше, курсорная арифметика — тестом T029. code-review диффа: механическая композиция T004-lifecycle + T030-курсор, новых багов нет.
- Блокеры: нет.

### 2026-07-11 — T032: Триггер custom_field_changed (per-field, doorbell)
- Статус: done
- Изменения: новый `src/lib/triggers/custom-field-changed.ts` через doorbell-фабрику T031: props `entity` (StaticDropdown leads/contacts/companies, default leads) + `field_id` (`Property.Dropdown`, refresher `['entity']`, options из `customFieldsUtils.fetchCustomFieldsMeta({ auth: auth.props, entity })` — label `field.name`, value `field.id`). `webhookEventsFromProps` → `update_lead`/`update_contact`/`update_company` по entity; `eventTypesFromProps` → `['custom_field_' + field_id + '_value_changed']`. Выход — событие целиком (value_before/after). ponytail-коммент про эксклюзивность per-field фильтра; в description — предупреждение о цикле «триггер + action на то же поле» и что полная сущность добирается через find_entity (filter[id]).
- Расширение фабрики: `events-doorbell.ts` получил `webhookEventsFromProps?` симметрично уже существующему `eventTypesFromProps` (нужно, т.к. подписка на update-вебхук зависит от выбранной entity; статический `webhookEvents` этого не давал). `webhookEvents` стал опциональным, добавлен helper `resolveWebhookEvents`. Других потребителей doorbell-фабрики нет — ничего не сломано.
- Решения: (1) entity-сужение `filter[entity]` в fetchEvents НЕ добавляю — id кастом-поля глобально уникален в аккаунте, тип `custom_field_{id}_value_changed` уже однозначен (подтверждено смоуком). (2) field_id-дропдаун держу инлайн в файле триггера (специфичен только этому триггеру), не выношу в props.ts.
- Живой e2e-smoke на стенде dzenteamdev (обязателен по спеке): PATCH `/leads/36632537` custom_fields_values field_id=829516 (numeric «Кол-во Гостей») value 42→87; через ~6 c `GET /events?filter[entity][]=lead&filter[entity_id][]=36632537&filter[type][]=custom_field_829516_value_changed` вернул событие. Подтверждено: `id` — ULID-строка (`01kx888b4tnvrftjmfzga5tsw5`); `type` = ровно `custom_field_829516_value_changed` (= то, что строит `eventTypesFromProps`); связка фильтров entity+entity_id+type работает; задержка ленты <6 c. Реальная форма value_after/before: `[{ custom_field_value: { field_id, field_type, enum_id, text, is_masked } }]` (значение под `text`, НЕ `value`) — sampleData обновлён под эту форму с реального события.
- Команды: `npx turbo run lint --filter=@activepieces/piece-amocrm` — pass (5/5); `npx turbo run build --filter=@activepieces/piece-amocrm` — pass (5/5); `npx vitest run .../events.test.ts` — 9/9 passed (фабрику трогал, курсорная логика не менялась); `npm run lint-dev` — exit 0, 0 errors (72 предсуществующих web-warning вне скоупа). Токен-чек `git diff --cached | grep -c eyJ0` = 0.
- code-review (low): 2 находки, обе guarded-only, фиксов не требуют — (a) `eventTypesFromProps` при незаполненном field_id даст `custom_field_undefined_value_changed`, но field_id required, run/enable без него не стартуют, test() деградирует до [sampleData]; (b) `resolveWebhookEvents` при отсутствии обоих опций вернёт `[]` — гипотетический будущий потребитель, текущие все передают одну из опций.
- Блокеры: нет.

### 2026-07-11 — T033: Триггеры budget_changed + lead_entered_stage (doorbell)
- Статус: done
- Изменения: `src/lib/triggers/budget-changed.ts` (doorbell-фабрика T031: `webhookEvents:['update_lead']`, `eventTypes:['sale_field_changed']`, без props, выход — событие целиком); `src/lib/triggers/lead-entered-stage.ts` (`webhookEvents:['status_lead']`, `eventTypes:['lead_status_changed']`, props `pipelineId` (pipelineDropdown, required) + `statusId` (statusDropdown, refresher pipelineId, required); `filterEvent` — событие проходит, если `value_after[0].lead_status.id === statusId`, сравнение через `String()` (устойчиво к number/string id дропдауна); хелперы `isRecord`/`enteredSelectedStatus` — гварды без any/as); регистрация обоих в `triggers/index.ts`; `src/i18n/translation.json` (+4 identity-ключа: 2 displayName + 2 description).
- Живой replay на стенде dzenteamdev (сделка 36632537): `PATCH /leads/36632537 {status_id:77124942,pipeline_id:9670378,price:77500}` → 200; через ~12 c `GET /events?filter[entity][]=lead&filter[entity_id][]=36632537` вернул оба события. Формы сняты с реальных событий (в них и sampleData):
  - `sale_field_changed`: `value_after:[{sale_field_value:{sale:77500,sale_with_minor_units:77500.0}}]`, `value_before:[{...:76000}]`, `created_by:0`, id ULID `01kx88gvk5...`.
  - `lead_status_changed`: `value_after:[{lead_status:{id:77124942,pipeline_id:9670378}}]`, `value_before:[{lead_status:{id:143,pipeline_id:9670378}}]`, id ULID `01kx88gvkr...`. Подтверждает форму фильтра `filterEvent` (value_after[0].lead_status.id). Тип события смены статуса — ровно `lead_status_changed` (совпал со спекой), НЕ `lead_status_lead` и т.п.
  - Замечание curl: брекеты amo-фильтров в URL требуют `curl -g` (zsh globbing), иначе exit 3 / пустое тело — на код piece не влияет (там URLSearchParams).
- Команды: `npx turbo run lint build --filter=@activepieces/piece-amocrm` — pass (6/6); `npm run lint-dev` — 30/30, 0 errors (те же предсуществующие web-warning вне скоупа). Токен-чек `git diff --cached | grep -c eyJ0` = 0. `git status` — только 4 разрешённых файла.
- Верификация: pass. code-review (low) по диффу: 1 находка — нарушение file-order из CLAUDE.md (хелперы стояли до экспортируемой const), исправлено (хелперы перенесены под `leadEnteredStage`); повторный lint+build зелёный. Рантайм-багов не найдено (механическая композиция doorbell-фабрики T031, курсор/lifecycle покрыты тестом T029 и живым смоуком T031).
- Блокеры: нет.

### 2026-07-11 — V006: чекпоинт-валидация блока T029–T034
- Статус: done (с фиксами)
- **Полные проверки (факты):** `npx turbo run lint build --filter=@activepieces/piece-amocrm` — pass (6/6). `npm run lint-dev` — 30/30, 0 errors. `npm run test-unit` — engine/shared/pieces зелёные, НО 8 падений в `packages/web` (`utils-schema.test.ts`, `output-table-view.test.ts`) — предсуществующие: тест-файлы не менялись с initial commit, ветка web не трогала (`git log main..HEAD -- packages/web` пуст). Вне скоупа, не чинил. `npx vitest run .../events.test.ts` — 9/9. `git status` — чист (до фиксов). Токен-чек токен-чек на утечку JWT (`git diff` по префиксу токена) = 0.
- **Структура piece:** 25/25 экшенов и 22/22 триггеров зарегистрированы (файлы ↔ index ↔ массивы; 3 незарегистрированных файла в actions/ — хелперы `*-payload`, ожидаемо); дублей `name` нет; i18n identity — 0 missing по новому блоку; `any`/`as` в новых файлах — чисто.
- **Скоуп `git diff main`:** кроме amocrm/ralph/tsconfig.base.json в диффе `.agents/designs/amocrm-piece-backlog.md`, `.claude/settings.json`, `.claude/scheduled_tasks.lock`, `.gitignore`, `ralph/ralph.sh` — ВСЕ из setup-коммитов оркестратора (792b25c3, c34c7e0e, 8002b9eb), не из итераций. Нарушений итерациями нет.
- **Сверка со спеками (sonnet-верификатор) + code-review (low):** пункты 1–9 чек-листа PASS. Найдено и ИСПРАВЛЕНО в этой итерации:
  1. (спека) отсутствовал обязательный ponytail-коммент в `events.ts` об отказе от pollingHelper/DedupeStrategy — добавлен, вместе с документацией потолка «DESC + maxPages»: лента `/events` отдаёт только desc (`order[created_at]=asc` проверен живьём — игнорируется), бурст >maxPages*limit (~500) событий между тиками теряет старейший хвост при сдвиге курсора. Чистого фикса без ASC на стороне amo нет; нереально на аккаунте с лимитом ~7 rps — задокументировано как ceiling (совпавшая находка моего code-review и верификатора).
  2. (баг UX) `find_events`: `entity_type='tasks'` оставлял `entity_id`-дропдаун навсегда disabled — `entityByTypeDropdown`/`fetchEntityOptions` в props.ts расширены на `tasks` (live-проверка: `GET /tasks?limit=250&order[updated_at]=desc` → 200; label задач из `text` — fallback в `toOptions`).
  3. (косметика) label `created_by` в find_events показывал «Responsible User» — `userDropdown` получил опциональный `displayName`, find_events передаёт `Created By` (+ i18n-ключ).
  Не чинил (осознанно): дублирование webhook subscribe/unsubscribe между events-doorbell.ts и webhooks.ts — «реюз механики» одинаковой формы, выделение общего хелпера ради 10 строк не оправдано.
- **Особое внимание V006:** курсор инициализируется now при enable (обе фабрики, from=0 невозможен) — PASS; дедуп границы секунды покрыт тестами (9/9) — PASS; курсор не двигается при пустой дельте doorbell (short-circuit до store.put) — PASS; per-field эксклюзивность задокументирована (ponytail в custom-field-changed.ts) и валидируется в find_events (regex + читаемая ошибка) — PASS; lifecycle вебхуков симметричен — PASS; живые smoke-факты (ULID id, desc-порядок, задержка ленты <6с, покрытие update_lead) зафиксированы в записях T029–T033 — PASS.
- **Пост-фикс верификация:** lint+build piece — pass (6/6, после правок); events.test.ts — 9/9; `npm run lint-dev` — 30/30, 0 errors; токен-чек = 0.
- Блокеры: нет.

### 2026-07-11 — T035: Poll-триггеры звонков incoming_call / outgoing_call
- Статус: done
- Изменения: `src/lib/triggers/incoming-call.ts` + `outgoing-call.ts` (через poll-фабрику T030 `createAmoEventsPollingTrigger`: `types:['incoming_call']` / `['outgoing_call']`; props `created_by` (`userDropdown({required:false, displayName:'Manager'})`) — опц. фильтр по менеджеру; `filterEvent` = `matchesManager` (пустой created_by → пропустить всё, иначе `String(event.created_by)===String(created_by)`); в description — требование подключённой телефонии + задержка до 1 мин); регистрация обоих в `triggers/index.ts`; `src/i18n/translation.json` (+3 identity-ключа: 2 description + 'Manager'; 'Incoming Call'/'Outgoing Call' уже были).
- Расширение фабрики: `events-polling.ts` получил опциональный `filterEvent` (симметрично doorbell-фабрике T031: `fresh.filter((e)=>filterEvent(e, propsValue))` в `run`, тип в params). Poll-фабрика раньше filterEvent не поддерживала — звонковый фильтр по менеджеру это первый потребитель. Курсорная арифметика не тронута.
- Живая проверка формы payload: НЕ УДАЛОСЬ — на стенде dzenteamdev телефония не подключена. `GET /events?filter[type][]=incoming_call&filter[type][]=outgoing_call` → HTTP 204 (пусто). Ключи типов валидны: `GET /events/types` содержит и `incoming_call`, и `outgoing_call`. sampleData обоих триггеров — **best-guess по докам amoCRM (звонок = note типа call_in/call_out, value_after[0].note.params с uniq/duration/phone), НЕ проверено живьём** — помечено ponytail-комментом в обоих файлах. Задачу НЕ блокирую (спека это прямо разрешает).
- Команды: `npx turbo run lint build --filter=@activepieces/piece-amocrm` — pass (6/6); `npm run lint-dev` — 30/30, 0 errors (72 предсуществующих web-warning вне скоупа); `npx vitest run src/lib/common/events.test.ts` — 9/9 (трогал poll-фабрику, курсор не менялся); токен-чек `git diff --cached | grep -c eyJ0` = 0; `git status` — только 5 разрешённых файлов.
- Верификация: pass. code-review (low) по диффу — находок нет (механическая композиция poll-фабрики T030 + filterEvent-паттерн T031; matchesManager дублируется в 2 файлах намеренно — file-per-trigger). Коммит 502cb5f7 (subject lowercase t035 — commitlint отверг T035 uppercase).
- Блокеры: нет.

### 2026-07-11 — T036: Триггеры тегов entity_tag_added / entity_tag_deleted (doorbell)
- Статус: done
- Механизм: **doorbell-фабрика** (createAmoDoorbellTrigger, T031). Обоснование — smoke T031 подтвердил, что смена тегов будит `update_lead` (вебхук пришёл на добавление тега), а тип `entity_tag_added` индексируется в ленте `/events` за секунды. Poll-фолбэк не нужен.
- Изменения: `src/lib/triggers/entity-tag-added.ts` (`webhookEventsFromProps` по entity, `eventTypes:['entity_tag_added']`, `filterEvent`); `entity-tag-deleted.ts` (симметрично, `['entity_tag_deleted']`); общий хелпер `src/lib/common/entity-tag.ts` (`entityTagUtils`: entityProperty StaticDropdown leads/contacts/companies default leads, tagNameProperty ShortText optional, webhookEvent map, matchesEntityAndTag); регистрация обоих в `triggers/index.ts`; `src/i18n/translation.json` (+7 identity-ключей: 2 displayName + 2 description + entity/tagName описания + 'Tag Name').
- Живой replay на стенде dzenteamdev (сделка 36632537) — обе формы сняты с реальных событий (в sampleData именно они):
  - `entity_tag_added`: PATCH `_embedded.tags:[{name:'t036-added'}]` → 200; через ~8с событие `value_after:[{tag:{name:'t036-added'}}]`, `value_before:[]`, id ULID `01kx8a71hh...`, `entity_type:'lead'`, `created_by:0`.
  - `entity_tag_deleted`: read-modify-write тегов (получил текущие, убрал t036-added, PATCH) → 200; событие `value_before:[{tag:{name:'t036-added'}}]`, `value_after:[]`, id `01kx8a7nbg...`. Тег в ленте несёт только `name` (без id).
- code-review (low) по диффу — 2 находки:
  1. **CONFIRMED, ИСПРАВЛЕНО:** filterEvent игнорировал выбранную entity — doorbell тянет `entity_tag_added` по всей ленте без entity-фильтра (фабрика не entity-scoped), подписка на contacts могла выдать событие тега лида из того же окна. Фикс: `matchesEntityAndTag` дополнительно гейтит по `event.entity_type` (map leads→lead/contacts→contact/companies→company). Оба триггера передают entity в filterEvent.
  2. **PLAUSIBLE, не чинил:** ENTITY_WEBHOOK_EVENT дублирует такую же карту в custom-field-changed.ts — две маленькие карты в разных файлах, выделение общего модуля ради 3 строк не оправдано (совпадает с file-per-trigger дублированием T033/T035).
- Команды: `npx turbo run lint build --filter=@activepieces/piece-amocrm` — pass (6/6, после снятия unused-import и после фикса находки 1); `npm run lint-dev` — 30/30, 0 errors (72 предсуществующих web-warning вне скоупа); токен-чек `git diff | grep -c eyJ0` = 0; `git status` — только 5 разрешённых файлов. Юнит-тест не добавлял — filterEvent-логика следует конвенции соседних триггеров (T033/T035 тоже без тестов), формы проверены живым replay, курсор покрыт events.test.ts.
- Верификация: pass. Коммит 48683a9a.
- Блокеры: нет.
