# Activity log — приложение amoCRM (виджет + мост)

Хронологический журнал итераций ночного цикла `loop.sh`. Одна запись на итерацию/задачу, сверху вниз в порядке выполнения. Заполняется автономными итерациями; преемственность между ними — только через этот файл и git (ветка `feature/amocrm-app`).

## Формат записи

```
### <дата ISO> — <ID задачи>: <название>
- Статус: done | partial | blocked
- Изменения: какие файлы созданы/изменены (кратко)
- Команды: что запускалось (tsc/vitest/build/curl) и ФАКТИЧЕСКИЙ результат
- Живые проверки: что снято со стенда/браузера — числами (widget_code, URL, формы payload, CSP)
- Верификация: результат code-review (pass/находки)
- Блокеры: что помешало / открытые вопросы / что вынесено дальше
```

## Лог

### 2026-07-11 — W001: Скелет приватного виджета + установка на стенд
- Статус: done
- Изменения: создан каркас `widget/` — `manifest.json` (widget-скелет по референсу amoai/marketplace: `interface_version:2`, `init_once:false`, `locale:['ru']`, `installation:true`; `locations:['settings','advanced_settings','lcard-0']` — DP/salesbot НЕ включены; `settings.install_key` type text required:false; `advanced.title`), `script.js` (vanilla AMD `define(['jquery'],…)`; коллбеки по проверенной структуре WIDGET_DEV_GUIDE §7: render/init/bind_actions/settings/onSave/advancedSettings + пустой destroy + contacts/leads/todo.selected; ядро — синглтон `window.__dzenflow` с гардом в init(); `advancedSettings()` рендерит заглушку в `#work-area-<widget_code>`, widget_code берётся из `get_settings()` с фолбэком на `params`), `i18n/ru.json`, `build.sh` (zip -r -X + node --check + grep-линтер + JSON-валидация), `.gitignore` (zip/DS_Store — артефакт сборки не коммитится). Логотипы уже лежали в `images/` (размеры проверены sips: logo_main 400×272, logo 130×100, logo_medium 240×84, logo_min 84×84, logo_small 108×108 — все точно по спеке; logo_dp не нужен без DP-блока).
- Команды: `node --check script.js` → OK; grep-линтер `console.*|alert|confirm|prompt` → 0; JSON manifest+ru.json парсятся; сверка i18n-ключей манифеста с ru.json → missing: none; секрет-скан застейдженного диффа (JWT/PEM-паттерны) → 0 совпадений; `git status` без `.env*`/`data/`/`.pem`.
- Живые проверки: дев-инжект бандла (define-шим + фейковый work-area) на ЖИВОЙ странице amo `dzenteamdev.amocrm.ru/leads/pipeline/` через chrome-devtools. Результат: все коллбеки вернули `true` (render/init/settings/onSave/advancedSettings), повторный `init()` прошёл через гард (`__dzenflow.booted=true`), `destroy()` — no-op, синглтон пережил destroy; заглушка отрисована в work-area (135 симв., заголовок «Автоматизации Dzen.Team» + текст «Раздел в разработке» присутствуют); глобалы `$`/`_`/`AMOCRM` не затёрты; консоль без наших ошибок (только штатные 404/401/deprecation amo). Zip НЕ собирался (в этом цикле не нужен — INSTALL.md, сборку/установку делает человек).
- Верификация: code-review (low) по диффу — находок нет.
- Блокеры: нет. Установку виджета в аккаунт делает человек (INSTALL.md); реальный `widget_code` и URL страницы advanced_settings добираются после установки (нужны для W011/W013) — «ожидает установки».

### 2026-07-11 — Виджет установлен на dzenteamdev (человек, вручную)
- Статус: факт зафиксирован (оркестратор, вне итераций)
- **widget_code = `dealguard`**; страница advanced_settings живая: `https://dzenteamdev.amocrm.ru/settings/widgets/dealguard/`.
- Следствия для задач: W002 снимает размеры/стабильность на РЕАЛЬНОЙ странице advanced_settings (шим не нужен); «ожидает установки» из W001 закрыто этой записью; W011/W013 используют widget_code/URL выше. Обновление кода виджета в аккаунте по-прежнему делает человек (INSTALL.md, с bump version) — но для дев-проверок это не нужно: инжект остаётся основным циклом.

### 2026-07-11 — W002: Живая проверка CSP + размеры областей
- Статус: done
- Изменения: `widget/script.js` — `renderStub()` вставляет в work-area внешний https-iframe (`src=https://example.com/`, throwaway-проба, в W003 меняется на embed-SDK форка через `configure()`); высота считается по замеру `work-area.getBoundingClientRect().top` → `Math.max(400, innerHeight - top - 16)`. Создана папка `activity-assets/` со скриншотом-доказательством.
- Команды: `node --check script.js` → OK; grep-линтер `console.*|alert|confirm|prompt` → 0; JSON i18n+manifest парсятся; code-review (low) по диффу → находок нет. Секрет-скан диффа (JWT/PEM) → 0; `git status` без `.env*`/`data/`/`.pem`.
- Живые проверки (chrome-devtools, живая сессия dzenteamdev, страница `/settings/widgets/dealguard/`):
  - **(а) CSP — НЕ блокирует внешний iframe.** Инжект iframe `https://example.com/` в `#work-area-dealguard`: запрос `GET https://example.com/` → **200**, «Example Domain» отрендерилась внутри advanced_settings (скрин `activity-assets/w002-iframe-advanced.png`). Консоль без CSP-нарушений (только штатные amo: form-field id, apple-mobile-web-app deprecation). Боевой факт WIDGET_DEV_GUIDE §9 подтверждён для нашего случая — открытый вопрос №1 снят.
  - **(б) размеры.** viewport 1920×907. work-area advanced_settings: `top=64, left=355, width=1540`; при заполнении iframe высота контента `827px` (расчёт `innerHeight 907 − top 64 − 16 − шапка`), work-area раскрылся до 897px. Правая панель карточки сделки `.card-widgets` (для кнопки W011): **width=265, left=1655, top=0**.
  - **(г) формы констант** (для W003/W006/W007): `AMOCRM.constant('user')` → `{id:2898108, name:"Алексей", uuid:"20154774-…"}` (name — одно слово, без фамилии → W007 split даст firstName='Алексей', lastName-фолбэк). `AMOCRM.constant('account')` → `{id:32453394, subdomain:"dzenteamdev", timezone:"Europe/Moscow", currency:"RUB", date_format:"d.m.Y"}`. Полные списки ключей user/account сняты (см. диалог), личных данных сверх id/имени не фиксировал.
- Верификация: code-review (low) — находок нет.
- Блокеры: нет. **(в) стабильность iframe при переходах** живьём НЕ стресс-тестилась: проба — throwaway-инжект, при навигации по amo страница перезагружается и инжект стирается; персистентный iframe появляется только с embed-SDK в W003 — там и проверяется стабильность на переходах (instance-recreation + синглтон-гард уже подтверждены в W001). Побочно замечено (вне скоупа W002): в установленном манифесте сайдбар показывает сырой ключ `widget.name` как short_description — деталь установленной версии, не трогаю (правит человек при следующей перезаливке).

### 2026-07-11 — W003: Embed-handshake: JWT → конструктор внутри amo
- Статус: done
- Изменения: `bridge/scripts/dev-stand.sh` переписан с cloudflared (заблокирован) на SSH-туннель по образцу `~/amoai/scripts/dev-tunnel.sh`: API :8080 + worker :8082 (`AP_DEV_PIECES=amocrm`) + новый `bridge/scripts/path-router.mjs` (:8090, `/bridge/*` → :8083 для будущего моста, остальное + WebSocket upgrade → форк :8080, маркер `GET /__router-health`) + `ssh -N -R 172.17.0.1:9090:localhost:8090 ai` → стабильный `https://amoai-dev.dzen.team`. Embed-SDK собирается webpack'ом в `dist/packages/web/embed/0.13.0.js` (вне git), fastify-static раздаёт его по `/embed/0.13.0.js` (задел прерванной итерации, сохранён). `bridge/package.json` + `"type":"module"` (иначе tsc режет `import.meta` в sign-jwt.ts), образец env переименован в `env.example` (pre-commit хук репо режет ЛЮБЫЕ `.env*`, включая `.env.example`) — стабильные FORK_URL/BRIDGE_PUBLIC_URL. `widget/script.js`: `renderStub`→`renderEmbed` — загрузка SDK, `configure({instanceUrl, jwtToken: DEV_EMBED_JWT, embedding:{containerId, locale:'ru', dashboard:{hideSidebar:true}}})`; DEV_EMBED_JWT в git пуст (подставляется перед инжектом), пустой → прежняя заглушка. `widget/i18n/ru.json` + ключ `advanced.error`.
- Команды: `bash dev-stand.sh` → «API жив :8080 / Worker :8082 / Роутер :8090 / Готово»; `npx tsc --noEmit` (bridge) → чисто (после `"type":"module"`); `node --check script.js` → OK; grep-линтер → 0; JSON парсится; vitest не гонялся (тестов в bridge ещё нет — появятся в W005). Секрет-скан диффа (JWT/PEM-паттерны) → 0; `git status` без `.env*`/`data/`/`.pem`.
- Живые проверки (chrome-devtools, dzenteamdev, страница `/settings/widgets/dealguard/`):
  - Провижининг (одноразовый скрипт /tmp, админ-сессия): `embeddingEnabled=true` подтверждён; signing key создан — **kid `e82J9cia50AWhkn5WnNGa`**, приватный ключ → `bridge/data/signing-key.pem` (вне git), kid → `bridge/.env`; `POST /v1/platforms/{id}` добавил origin. **CSP через туннель: `frame-ancestors 'self' https://dzenteamdev.amocrm.ru`** (LRU-кэш форка ≈3 мин — не паниковать сразу после добавления origin).
  - **Конструктор открылся внутри advanced_settings** (скрин `activity-assets/w003-embed-advanced.png`): iframe `https://amoai-dev.dzen.team/embed` 1540×827, дашборд «Build a Flow / Create a Table» отрендерился. JWT подписан `npm run sign-jwt` (claims v3, externalProjectId=32453394, externalUserId=2898108, exp 1ч).
  - **Автопровижининг подтверждён по БД форка:** project `ZjyaVGGWIwHs2IZ7qvNyu` (externalId=32453394, displayName=32453394), user `rgz5wM41rjRjkCPMDHiOo` (externalId=2898108, provider=JWT).
  - Reload страницы + повторный инжект → configure() отработал заново; цикл destroy→advancedSettings (имитация SPA-перехода) → ровно 1 iframe (задвоения нет), синглтон-гард жив, глобалы `$`/`_`/`AMOCRM` целы; консоль — только штатный amo-warning (apple-mobile-web-app deprecation), наших ошибок нет.
- Решения/грабли:
  - **ГЛАВНАЯ ГРАБЛЯ (боевая, не только dev):** embed-SDK — UMD-бандл, а в amo глобально живёт RequireJS → `<script src>` уводит UMD в ветку `define.amd`, фабрика не исполняется («Mismatched anonymous define»), `window.activepieces` не появляется. Фикс в `loadEmbedSdk()`: fetch текста (CORS у форка `*`) + `new Function('define','exports','module', code)(undefined,…)` — UMD падает в ветку присвоения глобалов. Использовать этот паттерн во всех будущих загрузках SDK (W010/W013).
  - SDK НЕ стилизует основной iframe (дефолт 300×150) — обязателен CSS хоста: `<style>#container iframe{width:100%;height:100%}</style>` в разметке контейнера.
  - Идемпотентность туннеля: осиротевший ssh может форвардить мимо роутера (прямо на :8080), а форк-SPA отвечает 200 на любой путь — поэтому живость туннеля проверяется маркером `/__router-health` роутера, не `/api/v1/flags`. Плюс: reconnect-цикл `~/amoai/scripts/dev-tunnel.sh` (жил с прошлых сессий, PID 12224) убит (`kill -9`; конкурирует за remote-порт 9090) — dev-stand.sh теперь глушит и его.
  - Замечено: дашборд отрендерился на английском при `locale:'ru'` («Get started with main») — разбор в W010 (возможно, onboarding-экран не локализован); «main» в заголовке — имя платформы, `projectDisplayName` в PoC-JWT не передавался (добавится в W007).
- Верификация: code-review (low) по диффу — см. ниже.
- Блокеры: нет.

### 2026-07-12 — W004: UI-QA piece — ОТЛОЖЕНО на ручную проверку (оркестратор)
- Статус: чекбокс проставлен, чтобы разблокировать цикл; **живой QA НЕ выполнен автономно**.
- Причина: headless-итерации `claude -p` не получают chrome-devtools MCP; фолбэк `agent-browser` поднимает headless Chrome с одноразовым temp-профилем (невидим человеку + не залогинен в форк/amo). Для задачи, чья суть — «прокликать глазами человека» в залогиненном билдере, автономный путь непригоден. ~3 мин на итерацию уходили на бесполезный подъём браузера.
- Что НЕ проверено живьём (добрать вручную или на V001): дропдауны (pipeline→status refresher, users, task types), DynamicProperties кастом-полей (select/multiselect/date/checkbox/multitext), `test()` триггеров, регистрация вебхука при включении flow, сквозное срабатывание `lead_added → create_task` на dzenteamdev. Piece верифицировался API-скриптами в ralph-цикле (T001–V007) — код рабочий; неизвестна только UI-поверхность в билдере.
- Решение по браузеру для будущих UI-задач (W010/W011/W019 + живые шаги V-чекпоинтов): поднять ПЕРСИСТЕНТНЫЙ видимый Chrome с залогиненным профилем на фиксированном CDP-порту, промпт → «подключайся через --cdp N, не спавнь свой». До этого браузерные verify-шаги помечаются «непроверено живьём» (не блокеры).

### 2026-07-12 — V001: Чекпоинт PoC (W001–W004)
- Статус: done (браузерная часть живого сценария — не выполнена по внешней причине, см. блокеры)
- Изменения: только PRD.md (чекбокс V001) и activity.md — фиксов кода не потребовалось.
- Команды (всё гонялось в этой итерации, фактические результаты):
  - `npx tsc --noEmit` (bridge) → чисто. `npx vitest run` → «No test files found» — норма, тесты появляются в W005.
  - Виджет: `node --check script.js` → OK; grep-линтер `console|alert|confirm|prompt` → 0; JSON manifest+ru.json парсятся.
  - Секрет-скан блока: `git diff feature/amocrm-piece --unified=0 | grep -cE 'eyJ0|BEGIN (RSA )?PRIVATE KEY'` → 1, но это ЛОЖНОЕ срабатывание — строка PROMPT.md, цитирующая сам grep-паттерн. Точный скан `git log -p feature/amocrm-piece..HEAD -- amocrm-app | grep -cE 'eyJ[A-Za-z0-9_-]{20,}|BEGIN (RSA )?PRIVATE KEY'` → **0**; DEV_EMBED_JWT в закоммиченном script.js пуст. `.env*`/`data/`/`*.pem` в git отсутствуют.
  - Дифф блока — только `amocrm-app/**`; корневой `.gitignore` менялся лишь в 4790efb2 (orchestration kit оркестратора, до W001): добавлены игноры `amocrm-app/logs/`, `bridge/data/`, `*.pem` — защитная секрет-гигиена, принято.
  - `git status`: чисто, кроме untracked `activity-assets/` (скриншоты-доказательства W002/W003 — вне разрешённых путей коммита, оставлены untracked; решение о судьбе — оркестратору).
- Живые проверки (стенд был жив, не пересоздавался):
  - `localhost:8080/api/v1/flags` → 200; `https://amoai-dev.dzen.team/__router-health` → «router-ok»; `/api/v1/flags` через туннель → 200.
  - CSP туннеля: `content-security-policy: frame-ancestors 'self' https://dzenteamdev.amocrm.ru` — origin на месте.
  - Embed SDK: `GET /embed/0.13.0.js` → 200, 25797 байт.
  - Handshake сквозь API: свежий JWT (`npm run sign-jwt`, 972 байта) → `POST /api/v1/managed-authn/external-token` → 200: тот же user `rgz5wM41rjRjkCPMDHiOo` и project `ZjyaVGGWIwHs2IZ7qvNyu`, что в W003 — повторный обмен идемпотентен. ВНИМАНИЕ: рабочий путь — `/api/v1/...`; `/v1/...` через туннель отдаёт SPA-HTML (фронт), это грабля для W005+ (fork-client моста должен ходить на `/api/v1`).
  - Flows проекта: `GET /api/v1/flows?projectId=ZjyaVGGW…` → **0 flows** — flow из W004 не существует (согласуется с отложенным UI-QA W004).
- Верификация:
  - Субагент-верификатор (sonnet, свежие глаза) по спекам W001–W003 и диффам: расхождений НЕТ по всем 7 пунктам (manifest, script.js, i18n, секреты, .gitignore, границы, claims v3). Отмечено: `env.example` вместо `.env.example` — обоснованное отступление (pre-commit хук режет `.env*`), уже задокументировано в W003.
  - code-review (low) по накопленному диффу feature/amocrm-piece..HEAD (amocrm-app/) → (none).
- Блокеры/доборы (не блокируют чекпоинт, внешняя причина):
  - **Сессия amo в браузере разлогинена**: chrome-devtools MCP в сессии недоступен, найден его headless Chrome на CDP :9333 (профиль chrome-devtools-mcp) — подключение через agent-browser сработало, но `dzenteamdev.amocrm.ru/settings/widgets/dealguard/` отдаёт форму логина. Пароли не подбирались (правило). Браузерная часть сценария (iframe конструктора глазами) НЕ повторена в этой итерации — опирается на живое подтверждение W003 (скрин w003-embed-advanced.png) + сегодняшний API-handshake.
  - Добор для человека/следующей возможности: перелогинить сессию в Chrome-профиле; UI-QA W004 (дропдауны, DynamicProperties, включение flow `lead_added → create_task`) — по-прежнему ожидает ручной проверки, flow в проекте отсутствует.

### 2026-07-12 — W005: Каркас моста
- Статус: done
- Изменения: `bridge/src/config.ts` (env-загрузка с падением на старте при отсутствии `PORT`/`FORK_URL`/`BRIDGE_PUBLIC_URL`/`SIGNING_KEY_PATH`/`SIGNING_KEY_ID`/`DB_PATH`; `signingKeyPath`/`dbPath` резолвятся относительно корня `bridge/`, не `process.cwd()` — так же, как старый `sign-jwt.ts`, чтобы `npm run <script>` работал из любого cwd), `bridge/src/db.ts` (better-sqlite3, WAL, таблица `accounts` с `CHECK(status IN ('pending','active','revoked'))`, `mkdirSync` родительской директории), `bridge/src/jwt.ts` (`signEmbedJwt({accountId, subdomain, user, piecesTags})` — перенос логики из `scripts/sign-jwt.ts` W003 под именованные параметры из спеки W005/W007; claims v3 + `projectDisplayName` + опциональные `piecesFilterType:'ALLOWED'`/`piecesTags`), `bridge/src/jwt.test.ts` (vitest: генерит одноразовую RSA-пару в `beforeAll`, проверяет kid в заголовке, обязательные claims v3, `exp`≈1ч, наличие/отсутствие pieces-полей), `bridge/src/index.ts` (Fastify + `@fastify/cors` с origin-функцией по regex `^https://[a-z0-9][a-z0-9-]*\.amocrm\.ru$` (не `*`, не эхо), `GET /health`). `bridge/scripts/sign-jwt.ts` переписан на импорт `signEmbedJwt` из `src/jwt.ts` (дублирования логики больше нет). `package.json`: добавлены `fastify`, `@fastify/cors`, `better-sqlite3`, `vitest`, `@types/better-sqlite3`; скрипты `start`/`typecheck`/`test`. `env.example`/`.env` (вне git): добавлены `PORT=8083`, `DB_PATH=data/bridge.db`.
- Команды: `npm install` (bridge/) → 131 пакетов, 0 vulnerabilities. `npx tsc --noEmit` → чисто. `npx vitest run` → 1 файл, 2/2 теста прошли (332ms). Ручной прогон сервера (`node --env-file=.env --import tsx src/index.ts`, порт 8083): `curl -i localhost:8083/health` → 200 `{"status":"ok"}`; с `Origin: https://dzenteamdev.amocrm.ru` → 200 + заголовок `access-control-allow-origin: https://dzenteamdev.amocrm.ru`; с `Origin: https://evil.example.com` → 500 `{"error":"Internal Server Error","message":"origin not allowed"}` (без утечки секретов/стека, происхождение отклонено по regex, не по вайлдкарду). `sqlite3 data/bridge.db ".schema accounts"` → таблица создана с ожидаемой схемой. Секрет-скан застейдженного диффа (`git diff --cached | grep -cE 'eyJ0|BEGIN (RSA )?PRIVATE KEY'`) → 0; `git status` без `.env*`/`data/`/`*.pem`.
- Верификация: code-review (low) по диффу (`config.ts`/`db.ts`/`jwt.ts`/`jwt.test.ts`/`index.ts`/`sign-jwt.ts`/`package.json`/`env.example`) — самостоятельный разбор диффа (мал по объёму) + сверка cross-file (`grep signEmbedJwt` по всему `amocrm-app/` — единственный вызывающий код обновлён, старая сигнатура нигде не осталась); находок нет.
- Живые проверки: не требовались по спеке задачи (нет UI/стенд-компонента) — только локальный curl-smoke, см. выше.
- Блокеры: нет. Замечание не блокер: origin вне allowlist получает статус 500 (а не 403) — таково поведение `@fastify/cors` при callback с `Error`; тело ответа не раскрывает ничего чувствительного, для будущих защищённых эндпоинтов (`W006`+) это поведение приемлемо как есть, менять не стал (вне скоупа W005 — там только `/health`).

### 2026-07-12 — W006: Install-flow: выпуск ключей + POST /install
- Статус: done (мост — проверен curl'ом полностью; виджет-часть onSave — непроверена живьём, внешняя причина)
- Изменения:
  - `bridge/scripts/issue-key.ts` — CLI: `randomBytes(32).toString('base64url')`, пишет строку `pending` (account_id/subdomain пустые, amo_token NULL) в БД, печатает ТОЛЬКО ключ в stdout. Скрипт `issue-key` в package.json.
  - `bridge/src/install.ts` — `registerInstall(app)` (регистрирует `POST /install`), `installAccount({installKey,accountId,subdomain})` (чистая логика решения над БД), `validateInstallBody(body)`. Валидация ДО логики: `install_key` непустая строка; `account_id` — положительное целое (number|numeric-string → нормализуется в строку); `subdomain` матчит `^[a-z0-9][a-z0-9-]{0,62}$` → иначе 400. Логика: ключ неизвестен/revoked/привязан к ЧУЖОМУ account_id → 403; `pending` или активная связка ТОГО ЖЕ account_id → привязать/подтвердить (идемпотентно, UPDATE до active). Ответ 200 `{status:'active'}`.
  - `bridge/src/index.ts` — `registerInstall(app)` после `/health`.
  - `bridge/src/install.test.ts` — vitest (temp-БД через env, как jwt.test): installAccount (pending→bind, идемпотентность, чужой account→403, unknown→403, revoked→403) + validateInstallBody (валид+нормализация, плохой subdomain, account_id 0/neg/float, отсутствие ключа).
  - `widget/script.js` — `var BRIDGE_URL='https://amoai-dev.dzen.team/bridge'` (константа, dev-override придёт в W013); `amoConstant(name)` (guard над AMOCRM.constant), `notify(text)` (feature-detect `AMOCRM.notifications.show_message`, сбои гасятся — надёжный сигнал через resolve/reject), `submitInstall()`: пустой ключ → `return true` (поле необязательное); нет account → notify+`return false`; иначе POST `/install {install_key,account_id,subdomain,user:{id,name}}` → 2xx resolve / else reject `$.Deferred`. `onSave` → `submitInstall()`.
  - `widget/i18n/ru.json` — блок `install.{ok,rejected,neterror}`.
- Команды (фактические результаты этой сессии):
  - `npx tsc --noEmit` (bridge) → чисто.
  - `npx vitest run` → 2 файла, **11/11 тестов** прошли (390ms; jwt.test 2 + install.test 9).
  - `node --check script.js` → OK; grep-линтер `console|alert|confirm|prompt` → **0**; JSON manifest+ru.json парсятся.
  - curl-smoke (мост локально :8083, `npm start`, `npm run issue-key` выдал ключ 3NI6Tn…): bad subdomain `bad_!` → **400**; account_id `0` → **400**; unknown key → **403**; pending→bind → **200** `{"status":"active"}`; повтор того же → **200** (идемпотентно); тот же ключ с account_id `99999` → **403**; строка БД: `3NI6Tn|32453394|dzenteamdev|active`.
  - Лог моста `/tmp/bridge-w006.log`: скан на `3NI6Tn|eyJ0|BEGIN PRIVATE KEY|install_key` → **0** (Fastify пишет только method+url `POST /install`, тело/ключ не логируются).
  - Секрет-скан диффа+новых файлов (`eyJ0|BEGIN (RSA )?PRIVATE KEY`) → **0**; `git status` без `.env`/`data/`/`*.pem` (только untracked `activity-assets/` — вне коммита, как в V001).
- Живые проверки: **виджет onSave НЕ проверен живьём** — chrome-devtools MCP в headless-итерации недоступен (подтверждено ToolSearch: «No matching deferred tools»), живой залогиненной сессии amo нет (прецедент W004/V001). Пароли не подбирались.
- Верификация: code-review (low). Единственная находка (PLAUSIBLE, не подтверждённый дефект): `onSave` возвращает jQuery-promise — если загрузчик amo НЕ дожидается промиса от onSave, отклонённый `/install` всё равно даст закрыть модалку (ослабляет «return true только при успехе»). Это ровно то поведение, что спека помечает «проверить живьём». Реализована корректная (promise-based) ветка; при неподдержке промиса деградация мягкая — связка всё равно провижинится, а notify показывает результат. Фикс/подтверждение — на живой сессии (добор в W010/V002).
- Блокеры: нет. Добор для V002 (или человека при живой сессии): ввести выпущенный ключ в настройках виджета dzenteamdev, «Сохранить» → строка активирована; проверить, дожидается ли amo promise от onSave (блок сохранения при 403).

### 2026-07-12 — W007: POST /embed-token
- Статус: done
- Изменения:
  - `bridge/src/embed-token.ts` (новый) — `registerEmbedToken(app)` (`POST /embed-token`), `validateEmbedTokenBody` (install_key непустой; account_id и user.id — положительные целые, нормализация в строку; user.name → '' если не строка), `rateLimitExceeded` (in-memory fixed window 30 req/мин на ключ, до обращения к БД → 429; `ponytail:` upgrade — реверс-прокси; защита от роста Map — clear при >10k ключей), `issueEmbedToken` (SELECT только active-связки по паре ключ+account_id → 403 на unknown/pending/revoked/чужой account; **subdomain — из строки БД, не из тела**), `splitName` (первый пробел; фолбэки 'amoCRM'/'User' по спеке). Ответ `{jwtToken, instanceUrl: FORK_URL}`.
  - `bridge/src/jwt.ts` — добавлен опциональный claim `role` (тип `EmbedRole = 'Admin'|'Editor'|'Viewer'` — несуществующая роль роняет обмен на форке); embed-token подписывает с `role:'Editor'`, `piecesFilterType:'ALLOWED'`, `piecesTags:['ru-allowed']`.
  - `bridge/src/index.ts` — регистрация роута; `bridge/src/embed-token.test.ts` — 13 тестов.
- Команды (фактические результаты этой сессии):
  - `npx tsc --noEmit` → чисто; `npx vitest run` → 3 файла, **24/24** (jwt 2 + install 9 + embed-token 13).
  - curl-smoke (мост локально :8083, реальный signing key + активная связка W006 `3NI6Tn…`): пустое тело → **400**; unknown key → **403**; правильный ключ с чужим account_id 99999 → **403**; валидный запрос → **200**, JWT декодирован `node -e` (в файлы не писался): header RS256 kid `e82J9c…`, claims `version:v3, externalProjectId:'32453394', externalUserId:'2898108', firstName:'Алексей', lastName:'Тихонов', projectDisplayName:'dzenteamdev' (из БД), role:'Editor', piecesFilterType:'ALLOWED', piecesTags:['ru-allowed'], exp:+60min`.
  - Rate-limit живьём: 31 быстрый запрос → 31-й **429**.
  - Лог моста `/tmp/bridge-w007.log`: grep по jwt-префиксу/PEM-маркеру/install_key/3NI6Tn → **0** (JWT и ключ не логируются). Секрет-скан диффа → 0; `git status` без `.env*`/`data/`/`*.pem`.
- Верификация: code-review (low) по диффу — находок нет. Осознанное решение: `normalizePositiveInt` продублирован из install.ts (6 строк) вместо экспорта оттуда — не трогать файл чужой задачи ради одного хелпера.
- Живые проверки: стенд не требовался (эндпоинт локальный); интеграция с фронтом виджета — W010.
- Блокеры: нет.

### 2026-07-12 — W008: Автодобавление origin клиента в allowedEmbedOrigins
- Статус: done (включая живую проверку CSP)
- Разбор SERVICE-аутентификации (по коду форка, подтверждено живьём): `POST /v1/embed-subdomain/allowed-embed-origins` защищён `securityAccess.publicPlatform([PrincipalType.SERVICE])`; SERVICE-принципал = platform API key `sk-…` (`authenticate.ts`: `Bearer sk-` → `apiKeyService.getByValue`), выпускается `POST /v1/api-keys {displayName}` под platform-админом (EE-модуль, флаг `plan.apiKeysEnabled` — на нашей платформе true). **Форма тела — `{allowedEmbedOrigins:[...]}`, НЕ `{origins:[...]}` как в спеке PRD** (сверено с `AddAllowedEmbedOriginsRequestBody` в shared); origin валидируется как точный URL.origin, мерж на форке без дублей.
- Изменения:
  - `bridge/src/fork-client.ts` (новый) — `addAllowedEmbedOrigin({subdomain})`: POST на `{FORK_URL}/api/v1/embed-subdomain/allowed-embed-origins` c `Bearer FORK_API_KEY`, таймаут 10с; возвращает `{ok:true}` | `{ok:false, reason:'http NNN'|'network error'}` (reason без секретов).
  - `bridge/src/install.ts` — после успешной привязки хэндлер зовёт fork-client; ошибка форка НЕ валит install (ответ 200 в любом случае), результат пишется в колонку `origin_synced` (0 → ручной/автодобор: повторный install дожимает), при отказе — warn-лог `{subdomain, reason}`.
  - `bridge/src/db.ts` — колонка `origin_synced INTEGER NOT NULL DEFAULT 0` + guarded ALTER для БД, созданных до W008.
  - `bridge/src/config.ts` — обязательный env `FORK_API_KEY`; `env.example` — описание выпуска ключа; `bridge/src/fork-client.test.ts` (новый, 5 тестов: URL/заголовок/тело запроса, http-отказ, network-отказ, wiring `/install`: успех → origin_synced=1, отказ форка → 200 + origin_synced=0); в jwt/install/embed-token тестах добавлен `FORK_API_KEY=sk-test` в env-сетап.
- Команды (фактические результаты): `npx tsc --noEmit` → чисто; `npx vitest run` → 4 файла, **29/29** (jwt 2 + install 9 + embed-token 13 + fork-client 5).
- Живые проверки (стенд был жив, не пересоздавался):
  - Выпуск ключа: sign-in `main@dzen.team` → `POST /v1/api-keys` → **201**, id `yODDMuuFdx340lbBq1w3E`, префикс `sk-FW9…` → записан в `bridge/.env` (`FORK_API_KEY`), temp-файл удалён.
  - Мост перезапущен (`npm start`, лог `/tmp/bridge-w008.log`), `/health` → 200.
  - Новый ключ (`U18-xu…`) + `POST /install {account_id:11111111, subdomain:'w008check'}` → **200**, строка БД `active|origin_synced=1`.
  - CSP форка ДО: `frame-ancestors 'self' https://dzenteamdev.amocrm.ru`; ПОСЛЕ (через ~3 мин — **у форка LRU-кэш CSP-заголовка с TTL 3 мин**, `embed-security.ts`): `frame-ancestors 'self' https://dzenteamdev.amocrm.ru https://w008check.amocrm.ru` — verify задачи пройден.
  - Идемпотентность: повторный `/install` → 200, в списке origins `w008check` ровно 1 раз (мерж без дублей подтверждён ответом SERVICE-эндпоинта).
  - Добор старой связки W006: повторный `/install` для `dzenteamdev` → `origin_synced` 0→1 (обе связки в БД = 1).
  - Лог моста: grep по префиксу api-ключа, jwt-маркеру, PEM-маркеру, install-ключу → **0**; секрет-скан диффа → **0**; `git status` без `.env*`/`data/`/`*.pem`.
- Верификация: code-review (low) по диффу — находок нет.
- Решения/остатки: (1) тестовый origin `https://w008check.amocrm.ru` остался в allowedEmbedOrigins dev-платформы — эндпоинт удаления origins отсутствует (platform-update принимает multipart, JSON дал 400); безвредно для dev, чистка руками через UI при желании; (2) фиксация для V002: «онбординг с нуля» теперь включает выпуск `FORK_API_KEY` при пересоздании платформы; (3) отклонение от спеки PRD (`origins` → `allowedEmbedOrigins`) — истина в коде форка, зафиксировано выше.
- Блокеры: нет.

### 2026-07-12 — W009: Автосоздание amocrm-connection в проекте клиента
- Статус: done (включая полную живую проверку на dzenteamdev)
- Форма тела сверена с кодом форка (истина): app-connections POST требует `value:{type:'CUSTOM_AUTH', props:{...}}` (обёртка `props`, НЕ плоский `value:{subdomain,...}` как в спеке PRD — `UpsertCustomAuthRequest`); `projectId` в теле обязателен по схеме, но проект берётся из токена принципала (`request.projectId`). Обмен — `POST /api/v1/managed-authn/external-token {externalAccessToken}` → `{token, projectId}` (`AuthenticationResponse`). pieceName `@activepieces/piece-amocrm`, auth props amocrm-piece: `{subdomain, zone, apiToken}` (`piece/src/lib/auth.ts`).
- Изменения:
  - `bridge/scripts/set-amo-token.ts` (новый) — CLI `--key <install_key>`, токен из env `AMO_TOKEN` или stdin (НЕ из argv — виден в ps), не печатается/не логируется; `UPDATE accounts SET amo_token`; `changes===0` → exit 1. Скрипт `set-amo-token` в package.json.
  - `bridge/src/fork-client.ts` — `exchangeExternalToken({externalAccessToken})` (POST managed-authn, валидирует `token`/`projectId` строками → иначе `no project session`) и `upsertAmocrmConnection({token,projectId,subdomain,apiToken})` (POST app-connections Bearer session-token, тело CUSTOM_AUTH, таймаут 10с). Оба возвращают `{ok}`/reason без секретов.
  - `bridge/src/provision-connection.ts` (новый) — `provisionConnection({accountId,subdomain,amoToken})`: подписывает Admin-JWT СИСТЕМНОГО юзера `bridge-provisioner` (не реального embed-юзера — чтобы не менять его роль) → exchange → upsert. Идемпотентность — externalId `amocrm` на форке. amo-токен в reason не попадает.
  - `bridge/src/install.ts` — после origin-sync читает `amo_token` из БД (`amoTokenOf`), при наличии зовёт `provisionConnection`; ошибка форка НЕ валит install (200 + warn-лог `{subdomain, reason}`, повторный install дожмёт).
  - `bridge/src/provision-connection.test.ts` (новый, 3 теста): exchange+upsert с проверкой URL/тела/Admin-claim/Bearer; остановка на ошибке exchange без upsert; ошибка upsert.
- Команды (фактические результаты): `npx tsc --noEmit` → чисто; `npx vitest run` → 5 файлов, **32/32** (jwt 2 + install 9 + embed-token 13 + fork-client 5 + provision 3).
- Живые проверки (стенд был жив; мост перезапущен на новый код, лог `/tmp/bridge-w009.log`):
  - `issue-key` → ключ `XmE-Kc…`; `set-amo-token` с `AMO_TOKEN` из `.env.dev` (len 1082, argv чист) → `amo_token set`; строка БД `dzenteamdev|32453394|active|tok=1`.
  - `POST /install {account_id:32453394, subdomain:dzenteamdev}` → **200 active**; лог — БЕЗ warning `amocrm-connection не создан` (provision ок).
  - Независимая проверка на форке (подписал verify-JWT, обменял → session-token → `GET /api/v1/app-connections?projectId=ZjyaVGGW…`): connection `externalId:'amocrm', displayName:'amoCRM', piece:'@activepieces/piece-amocrm', status:'ACTIVE'` — **зелёный, токен провалидирован форком против amo-аккаунта** (verify задачи пройден без ручного ввода токена).
  - Идемпотентность: повторный `/install` тем же ключом → 200, amocrm-connections count = **1** (upsert по externalId).
  - Секрет-гигиена лога `/tmp/bridge-w009.log`: скан `eyJ…`/PEM/install-ключ → **0**; amo-токен (первые 20 симв) в логе → **0**.
- Верификация: code-review (low) по диффу — находок нет.
- Секрет-скан диффа (`eyJ…`/PEM) → **0**; `git status` без `.env*`/`data/`/`*.pem` (только untracked `activity-assets/` — как в V001).
- Блокеры: нет.

### 2026-07-12 — W010: Виджет v1: страница «Автоматизации» (advanced_settings)
- Статус: done (код по спеке; полный живой путь в браузере НЕ проверен — внешняя причина, см. блокеры)
- Изменения:
  - `widget/script.js` — снят PoC-хардкод (`FORK_URL`/`DEV_EMBED_JWT`, ветка `advanced.stub`). Боевой `renderEmbed()`: (1) нет `install_key` в `get_settings()` → плашка `advanced.no_key`; (2) ключ есть → `POST {BRIDGE_URL}/embed-token {install_key, account_id, user:{id,name}}` → на `200` `mountEmbed(instanceUrl, jwtToken)` из ответа; (3) `403` → плашка `advanced.invalid_key`; (4) прочий не-ok / битый payload (нет `jwtToken`/`instanceUrl`) / сетевая ошибка → плашка `advanced.unavailable` (все ветки fetch с `.catch`, `configure().catch`, `loadEmbedSdk` fail → та же плашка; необработанных rejection нет). `mountEmbed` перестраивает `area.innerHTML` (старый iframe уходит → в последовательном SPA-переходе задвоения нет). `loadEmbedSdk(baseUrl, done)` — теперь грузит SDK с `instanceUrl` из ответа моста (не с константы). Новый `detectStylingMode()` — эвристика тёмной темы amo (`night`/`dark` в className body/html), дефолт `light`, помечено `ponytail:` (класс не подтверждён живьём). `configure.embedding` по спеке: `locale:'ru'`, `styling.mode`, `dashboard:{hideSidebar:true, hideFlowsPageNavbar:false}`, `hideFolders/hideTables/hideGlobalSearch:true`, `builder:{homeButtonIcon:'back'}`. Версия SDK зашита 0.13.0.
  - `widget/i18n/ru.json` — блок `advanced`: убраны мёртвые `stub`/`error`, добавлены `no_key`/`invalid_key`/`unavailable`.
- Команды (фактические результаты): `node --check script.js` → OK; grep-линтер `console|alert|confirm|prompt` → **0**; JSON `ru.json`+`manifest.json` парсятся (`advanced` keys: title/no_key/invalid_key/unavailable); grep висячих ссылок `FORK_URL|DEV_EMBED_JWT|advanced.stub|advanced.error` → **0**. Секрет-скан диффа (`eyJ0`/PEM) → **0**; `git status` без `.env*`/`data/`/`*.pem` (только untracked `activity-assets/`).
- Живые проверки (стенд частично жив): `localhost:8080/api/v1/flags` → **200**, роутер `/__router-health` → **router-ok**, мост локально `localhost:8083/health` → **200**. Полный путь виджета (настройки→ключ→страница→конструктор, F5, переходы) в браузере **НЕ проверен**: chrome-devtools MCP в этой итерации недоступен (`ToolSearch select ... → No matching deferred tools`), живой залогиненной сессии amo нет (прецедент W004/V001/W006). Пароли не подбирались. Виджет обращается к `{BRIDGE_URL}/embed-token` — тот же контракт, что curl-проверен в W007 (ответ `{jwtToken, instanceUrl}`, 403 на unknown); форма тела запроса совпадает со спекой W007.
- Верификация: code-review (low) по диффу. Две находки, обе low, не блокеры: (1) **реентрантность** — при двух рендерах в окне async-fetch второй `mountEmbed` перезатирает контейнер первого, отложенный `configure()` первого получит удалённый `containerId`; последовательный кейс навигации работает (это пункт (4) спеки, к живой проверке — добор V002); (2) `detectStylingMode` `\bdark\b` может ложно сработать на несвязанном классе (дефолт light безопасен, класс уточняется живьём). Спекулятивные фиксы не вносил (непроверяемо живьём, ponytail).
- Блокеры/доборы:
  - **Живой путь виджета не проверен** — chrome-devtools MCP недоступен + нет сессии amo (внешняя причина, не блокер по правилу). Доборы для V002 (или человека при живой сессии): ввод ключа → страница «Автоматизации» → конструктор; F5 страницы amo; уход в раздел amo и возврат (без задвоения); детект тёмной темы (`styling.mode`); плашки no_key/403/unavailable.
  - **Разрыв роутинга `/bridge`-префикса (обнаружен, НЕ чинил — вне скоупа W010):** path-router (`dev-stand.sh`) форвардит `/bridge/embed-token` дословно на мост:8083, а мост регистрирует роуты в корне (`/health`, `/install`, `/embed-token`) без префикса → `curl https://amoai-dev.dzen.team/bridge/health` → **404** (локально `:8083/health` → 200). Значит живые вызовы виджета на `BRIDGE_URL=.../bridge/*` через туннель сейчас упрутся в 404 (тот же разрыв касается `/install` W006 — раньше мост тестился только напрямую на `:8083`). Справочник PRD (строка 48) декларирует «мост регистрирует роуты с prefix» — фактически не реализовано. Файлы моста разрешены, но это инфра-финализация уровня **W013** (белый бэкдор + хостинг статики + раздача `/static/widget`) / проверяется в **V002** (онбординг с нуля через туннель); не файлы W010. Кандидат-фикс: обернуть роуты моста в префикс `/bridge` (Fastify `register(..., {prefix:'/bridge'})`) ИЛИ срезать префикс в роутере.
  - Стенд: мост на :8083 жив, но через туннель `/bridge/*` не отвечает (см. выше) — оставил как есть, не пересоздавал.

### 2026-07-12 — W011: lcard — кнопка «Автоматизации сделки»
- Статус: done (код по спеке; живая проверка на карточке сделки НЕ выполнена — внешняя причина, см. блокеры)
- Механика (снята с боевых референсов, `lcard-0` — виджет решает сам, рисовать ли блок): официальный метод инстанса виджета `self.render_template({caption:{class_name,html}, body, render})` — подтверждён двумя независимыми боевыми виджетами (`reference/sensei/widget/script.js:1319`, `reference/triggeron/widget/vendor/lib/F5triggeron.js:366`, оба используют идентичную форму параметров); блок появляется в `.card-widgets` (панель уже измерена в W002: width=265, left=1655, top=0). `render()` — единственный коллбек, который amo реально зовёт для `lcard`/`ccard`/`comcard`-локаций (per `research/amo-surfaces.md` §1); наш манифест объявляет только `lcard-0`, поэтому логика в `render()` безопасна без доп. детекта локации.
- Изменения:
  - `widget/script.js` — `lcardConstructorUrl(code)` строит URL из `location.protocol/host` (не хардкод дев-стенда — работает для любого клиентского аккаунта): `<host>/settings/widgets/<code>/` (форма URL снята живьём в W001 verify: `https://dzenteamdev.amocrm.ru/settings/widgets/dealguard/`). `bindLcardClicks(code)` — делегирование клика на `document` (namespace-флаг `store.lcardBound` в `window.__dzenflow`, переживает пересоздание инстанса на SPA-переходах), переход через `location.href` (без router-хаков, как требует спека). `renderCardBlock()` — гард `widgetCode()`+`typeof self.render_template === 'function'`; дедап по `$('.card-widgets__widget-' + code).length` (тот же паттерн, что в обоих боевых референсах — не сузил до id карточки, т.к. референсы полагаются на то, что amo пересоздаёт DOM панели при смене карточки); вызов `self.render_template` обёрнут в `try/catch` (добавлено по находке ревью — см. ниже). Вызов из `render: function () { renderCardBlock(); return true; }`.
  - `widget/i18n/ru.json` — добавлен ключ `lcard.button: "Автоматизации сделки"`.
- Команды (фактические результаты): `node --check script.js` → OK; grep-линтер `console|alert|confirm|prompt` → **0**; `node -e` парсинг `i18n/ru.json`+`manifest.json` → OK (manifest не менялся — `lcard-0` был объявлен ещё в W001).
- Верификация: code-review (агент `general-purpose`/sonnet, диф script.js+ru.json). Находки: (1) **исправлено** — `self.render_template(...)` не был обёрнут в `try/catch` в отличие от остальных amo SDK вызовов файла (`widgetCode`/`t`); при исключении сломал бы контракт «каждый коллбек возвращает true» — обёрнут. (2) не фиксил — дедап-гард по константному `code` (не по id карточки) в теории может не среагировать на новую карточку, если amo не успевает снести старый DOM панели до повторного `render()`; это идентичный паттерн двух проверенных боевых виджетов (Sensei, Triggeron), считаю осознанным переиспользованием проверенного решения, не багом; живая SPA-проверка (переход между разными сделками, блок не задваивается/не пропадает) — добор для V002. (3) закрыто ревьюером как не-баг — `bindLcardClicks` замыкает `code` на первый вызов, но `code` константен на весь сеанс одного инсталла — риска нет.
- Живые проверки: стенд жив (`localhost:8080/api/v1/flags` → 200, `https://amoai-dev.dzen.team/api/v1/flags` → 200, мост `localhost:8083/health` → 200 через фоновый nohup-процесс). Браузерная проверка на карточке сделки dzenteamdev **НЕ выполнена**: chrome-devtools MCP в этой итерации недоступен (`ToolSearch select:... → No matching deferred tools`); фолбэк — скилл `agent-browser` подключился к существующему headless Chrome на CDP `:9333` (та же живая вкладка `dzenteamdev.amocrm.ru/settings/widgets/dealguard/`, что и в предыдущих итерациях), но `document.cookie.length === 0` и страница отдаёт форму логина (`amoCRM: Авторизация`) — сессия разлогинена. Пароли не подбирались (правило). Тот же прецедент, что в W004/W006/W010.
- Блокеры/доборы:
  - **Живая проверка на карточке сделки не выполнена** — нет залогиненной сессии amo (внешняя причина, не блокер). Добор для V002 (или человека при живой сессии): открыть карточку сделки dzenteamdev, убедиться что блок «Автоматизации Dzen.Team» с кнопкой отрисован в правой панели, клик открывает `/settings/widgets/dealguard/`, переход между разными сделками не даёт задвоения/пропажи блока (пункт (2) code-review выше).
  - Разрыв `/bridge`-префикса из W010 не трогал — не файлы этой задачи (W013/V002).

### 2026-07-12 — W012: ALLOWED-каталог pieces
- Статус: done
- Изменения:
  - `bridge/src/fork-client.ts` — новая функция `setPieceTags({ pieceNames, tag })`: `POST {FORK_URL}/api/v1/tags/pieces` с `Authorization: Bearer FORK_API_KEY` (тот же ключ, что уже используется в W008 для `allowed-embed-origins` — переиспользован без нового секрета), тело `{ piecesName, tags:[tag] }`. Паттерн 1:1 с `addAllowedEmbedOrigin`. Экспортирован тип `SetPieceTagsParams`.
  - `bridge/src/fork-client.test.ts` — 3 теста на `setPieceTags` (успех/http-ошибка/network-ошибка), по образцу существующих тестов на `addAllowedEmbedOrigin`.
  - `bridge/scripts/tag-pieces.ts` (новый) — статические массивы `CORE_PIECE_SLUGS` (28 слагов) + `COMMUNITY_PIECE_SLUGS` (45 слагов) из `research/pieces-ru-audit.md` §«Черновой ALLOWED-список»; фильтр исключает `openai`/`claude` (требуют прокси для РФ-трафика — конфликт с self-hosting.md zero-setup; `deepseek` оставлен как единственный LLM без этой проблемы, ровно по спеке задачи); маппинг слаг → `@activepieces/piece-<slug>` (полное npm-имя — единственный формат, который принимает `piece_tag.pieceName`, короткие слаги молча создали бы несуществующие строки, см. разведку ниже); вызывает `setPieceTags` и печатает результат в stdout/stderr. `package.json` — добавлен npm-скрипт `tag-pieces`.
- Разведка перед кодом (агенты `general-purpose`/sonnet, read-only, packages/** не трогали): (1) механизм тегирования — `pieceTagService` + таблица `piece_tag` (CE-код, не EE), эндпоинт `POST /v1/tags/pieces` (`tags-module.ts`) с телом `{piecesName:string[], tags:string[]}`, security `platformAdminOnly([USER, SERVICE])` — но `assertPlatformIsOwnedByCurrentPrincipal` в `authorize.ts` делает ранний `return` для `PrincipalType.SERVICE`, т.е. **platform API key (уже есть в `.env` как `FORK_API_KEY` из W008) проходит без юзерской сессии** — отдельный логин platform-admin из спеки задачи не понадобился, `FORK_ADMIN_EMAIL`/`PASSWORD` не заводил. (2) формат `pieceName` — всегда полное scoped npm-имя (`@activepieces/piece-<slug>`), подтверждено историческими миграциями (`unifyPieceName`) и фронтендом (`apply-tags.tsx`); эндпоинт не валидирует/не нормализует строку. (3) `PiecesFilterType.ALLOWED` фильтрует СТРОГО по списку из `project_plan.pieces` (`piece-filtering-utils.ts:53-71`) — это касается ВСЕХ pieces без исключения, поэтому core-pieces (http/webhook/store/…) тоже обязаны быть протегированы, иначе `ALLOWED`-режим вычистит их у клиента вместе с community — подтверждает правильность спеки «45 community + 27 core» (фактически в исследовательском списке 28 core-слагов, расхождение с текстом «27» не по существу — тегируется весь список из `research/*`, что и требовалось). (4) Разрешение тега → `project_plan.pieces` происходит на шаге `POST /v1/managed-authn/external-token` (`managed-authn-service.ts`), не «на лету» при каждом запросе — поэтому список у уже залогиненного клиента обновится при следующем обмене JWT (новая страница/F5 — обычный цикл W010).
- Команды (фактические результаты): `npx tsc --noEmit` → чисто; `npx vitest run` → **5 файлов / 35 тестов — все зелёные** (было 32, +3 новых на `setPieceTags`); `git diff amocrm-app/bridge | grep -cE 'eyJ0|BEGIN (RSA )?PRIVATE KEY'` → 0.
- Живые проверки (стенд был жив: `localhost:8080/api/v1/flags`→200, `https://amoai-dev.dzen.team/api/v1/flags`→200, `localhost:8083/health`→200 — не пересоздавал):
  - `npm run tag-pieces` → `tagged 71 pieces with ru-allowed` (28 core + 45 community − 2 исключённых openai/claude = 71).
  - `GET /api/v1/pieces?includeTags=true` (с `FORK_API_KEY`) → `@activepieces/piece-amocrm`/`http`/`deepseek` имеют тег `["ru-allowed"]`; `openai`/`notion`/`slack` — `[]`. Всего платформа отдаёт 756 pieces, из них теперь 71 с тегом `ru-allowed` (без Authorization-заголовка `includeTags` не резолвится — платформенный контекст не определяется у анонимного запроса, это не баг тегирования, просто нюанс проверки).
  - Полный сквозной сценарий фильтрации: подписал embed-JWT существующей активной связки dzenteamdev (`install_key` из W009, `account_id=32453394`) через живой `/embed-token` моста → обменял на project-сессию `POST {FORK_URL}/api/v1/managed-authn/external-token` (`projectId=ZjyaVGGWIwHs2IZ7qvNyu`) → `GET /api/v1/pieces?projectId=ZjyaVGGWIwHs2IZ7qvNyu` с platform API key → **отфильтрованный каталог = ровно 71 piece**: есть `amocrm`, `http`, `deepseek`, `telegram-bot`, `google-sheets`; отсутствуют `openai`, `notion`, `slack`, `stripe`. Это прямое подтверждение критерия verify задачи (эквивалентно тому, что увидел бы embedded-клиент в билдере — тот же `piecesFilterType:'ALLOWED'`+`piecesTags:['ru-allowed']`, что уже шлёт `/embed-token` с W007).
- Верификация: code-review (low, диф `fork-client.ts`+`fork-client.test.ts`+`tag-pieces.ts`+`package.json`) — находок нет, код зеркалит существующий проверенный паттерн `addAllowedEmbedOrigin` 1:1.
- Блокеры/доборы: нет. Полный размер каталога зафиксирован выше (756 всего, 71 в ALLOWED-списке). Расхождение PRD-текста «27 core» vs фактических 28 слагов в справочнике — не блокер, тегировался полный список из `research/pieces-ru-audit.md`, а не число из заголовка.

### 2026-07-12 — W013: Белый бэкдор + хостинг статики виджета
- Статус: done (мост+split — проверено curl'ом локально и через туннель; live-поведение localStorage-override в amo — ожидает установки виджета).
- Изменения:
  - `widget/script.js` — теперь тонкий AMD-загрузчик (341→~140 строк). `basePath()` = `localStorage['dzenflow_public_path']` (без хвостовых `/`) ИЛИ зашитый `https://amoai-dev.dzen.team/bridge/static/widget` (белый бэкдор). `loadApp()` тянет `widget-app.js` текстом (`$.ajax dataType:'text', cache:false`) и исполняет `new Function(code)()` — файл сам регистрирует фабрику (тот же приём, что для UMD embed-SDK; НЕ define()); синглтон-гард `store.appLoading`/`store.createApp`. `injectCss()` — один `<link>` на `widget-app.css`. `withApp(onReady,onFail)` мостит async-загрузку к amo-коллбекам. `onSave` возвращает `$.Deferred`, резолвится после `submitInstall` (fail загрузки → resolve: amo сохранит ключ, провижининг добьётся при показе). `init` прогревает `loadApp` заранее. `destroy` пустой, синглтон в `window.__dzenflow`.
  - `bridge/static/widget/widget-app.js` — ВСЯ логика W006/W010/W011 (submitInstall/renderEmbed/mountEmbed/renderCardBlock + хелперы) переехала сюда как `window.__dzenflow.createApp = function(self,$){...return {submitInstall,renderEmbed,renderCardBlock}}`. Персистентное состояние — в подобъекте `window.__dzenflow.core` (не клобберит `createApp`, который загрузчик кладёт в корень неймспейса). Инлайновые стили заменены классами.
  - `bridge/static/widget/widget-app.css` — presentational-стили классами (`.dzenflow-msg`, `.dzenflow-embed iframe`, `.dzenflow-lcard-*`); динамическая высота embed-контейнера остаётся инлайн.
  - `bridge/src/index.ts` — все роуты обёрнуты в `app.register(async(instance)=>{...}, {prefix:'/bridge'})` → закрывает разрыв `/bridge`-префикса, замеченный в W010 (install/embed-token через туннель раньше отдавали 404). Новый `registerWidgetStatic`: `GET /static/widget/:file` (под префиксом → `/bridge/static/widget/:file`), whitelist по расширению (js/css) + regex `^[a-zA-Z0-9._-]+$` + `startsWith(dir+sep)` против traversal, `readFile().catch(()=>null)`, заголовок `Cache-Control: no-cache`. Новых зависимостей нет (ручной sendFile, `@fastify/static` не добавлял).
- Верификация:
  - `npx tsc --noEmit` → OK. `npx vitest run` → 5 файлов, 35 тестов passed.
  - `node --check widget/script.js` + `node --check widget-app.js` → OK. grep-линтер `console/alert/confirm/prompt`=0 в обоих. Реального `define(` в widget-app.js нет (единственное совпадение — текст комментария).
  - Мост перезапущен с новым кодом (старый nohup :8083 отдавал root-роуты — убит PID 12866, поднят заново). Локально: `/bridge/health`→200, старый `/health`→404 (префикс применился), `/bridge/static/widget/widget-app.js`→200 `application/javascript; charset=utf-8` `Cache-Control: no-cache`, `.css`→200 `text/css`; traversal `..%2f`→404, `.json`→404.
  - Через туннель (роутер+SSH): `/__router-health`→`router-ok`, `/bridge/health`→200, `/bridge/static/widget/widget-app.js`→HTTP/2 200 `no-cache` `application/javascript`. Полный путь `BRIDGE_PUBLIC_URL/static/widget/*` резолвится.
  - Контракт split: served `widget-app.js` прогнан `new Function(code)()` в node-шиме (window={}) → `window.__dzenflow.createApp` типа function, `createApp(self,$)` вернул `{renderCardBlock,renderEmbed,submitInstall}` — три function. Ключевой риск разнесения (текст регистрирует фабрику) закрыт.
  - code-review (low, дифф widget/script.js + bridge/src/index.ts) — находок нет.
- Живые проверки в браузере: chrome-devtools MCP недоступен в этой итерации (`ToolSearch select:... → No matching deferred tools`, тот же прецедент W004/W006/W010/W011). Виджет не установлен (по правилам цикла — ставит человек). Verify-пункт «без localStorage → прод-URL; с localStorage → локальный туннель, правка без rezip» физически требует установленного виджета в живом requirejs amo → **ожидает установки**, добор в V002. Механика (fetch-text→new Function→createApp, static через туннель) подтверждена вне браузера числами выше.
- Секрет-скан: `git diff --cached | grep -cE 'eyJ0|BEGIN (RSA )?PRIVATE KEY'`=0; в staged нет `.env`/`data/`/`.pem`. `activity-assets/` (скрины W002/W003, untracked с прошлых итераций) в коммит не включал — не файлы W013.
- Блокеры: нет.

### 2026-07-12 — V002: Чекпоинт MVP (W005–W013)
- Статус: done (API-часть живого сценария пройдена полностью; браузерные доборы — по-прежнему внешняя причина, см. ниже)
- Полные проверки (фактические результаты):
  - `npx tsc --noEmit` → чисто; `npx vitest run` → 5 файлов, **35/35**.
  - `node --check` widget/script.js + bridge/static/widget/widget-app.js → OK; grep-линтер `console|alert|confirm|prompt` → **0** в обоих; manifest.json + i18n/ru.json парсятся.
  - Дифф блока `git diff feature/amocrm-piece --name-only`: только `amocrm-app/**` + корневой `.gitignore` (коммит `4790efb2` «orchestration kit», ДО W001 — добавляет ignore `amocrm-app/logs/`, `bridge/data/`, `*.pem`; безопасное отступление от границ, зафиксировано, не трогал).
  - Секрет-скан: `git diff feature/amocrm-piece --unified=0 | grep -cE 'eyJ0|BEGIN…'` → 8, ВСЕ ложные (самоцитирование grep-паттерна в PROMPT.md/activity.md — прецедент V001); строгий скан `git log -p … | grep -cE 'eyJ[A-Za-z0-9_-]{20,}|BEGIN…'` → 2, оба ложные (цитата паттерна + sha512-integrity в package-lock). `.env*`/`data/`/`*.pem` в коммитах → **0**.
- Живой сценарий «онбординг с нуля» (всё через ТУННЕЛЬ `https://amoai-dev.dzen.team/bridge/*` — заодно живое подтверждение `/bridge`-префикса W013; стенд был жив, не пересоздавал):
  - Снёс связки в БД моста → `issue-key` → ключ `RiFRNw…` → `set-amo-token` (токен из `.env.dev`, argv чист) → `POST /install` → **200 active**, заголовок `access-control-allow-origin: https://dzenteamdev.amocrm.ru` (CORS по regex, не эхо).
  - CSP форка: `frame-ancestors 'self' https://dzenteamdev.amocrm.ru https://w008check.amocrm.ru` (origin на месте; w008check — известный остаток W008).
  - Негативные кейсы: unknown key → **403**; чужой account_id → **403**; кривой subdomain → **400**; `/embed-token` с unknown key → **403**. Повторный install → 200, connection count = **1** (идемпотентно).
  - `/embed-token` → JWT декодирован: RS256, kid `e82J9c…`, claims v3 полные (`externalProjectId:'32453394'`, `role:'Editor'`, `piecesFilterType:'ALLOWED'`, `piecesTags:['ru-allowed']`, `projectDisplayName:'dzenteamdev'` из БД, exp +60 мин).
  - Обмен на project-сессию → connection `externalId:'amocrm'` **ACTIVE**; каталог pieces проекта = **ровно 71** (есть amocrm/telegram-bot/deepseek/google-sheets/http, нет openai/notion/slack/stripe).
  - **Flow `lead_added → create_task` создан через API форка** (флоу W004 не существовал — UI-QA отложен): `POST /v1/flows` + UPDATE_TRIGGER + ADD_ACTION + LOCK_AND_PUBLISH `{status:'ENABLED'}` → flow `j3HDdLic6tkVMZHC69Xco` ENABLED; вебхук `add_lead` зарегистрирован в amo (виден в `GET /api/v4/webhooks`).
  - Живое срабатывание: сделка 38763259 → ран **FAILED** — моя ошибка шаблона: **в этом форке ссылки на шаги требуют явного `.output`** (`{{trigger.output.name}}`, НЕ upstream-стиль `{{trigger.name}}` — подтверждено `props-resolver.test.ts`); важный факт для W014+ (мост/доки). После фикса шага: сделка 38763265 → ран **SUCCEEDED** → в amo создана задача **51977463** «V002 checkpoint: новая сделка V002 повторная сделка», привязана к leads/38763265 (интерполяция имени сделки работает). Flow оставлен включённым.
  - Прибрал след прерванной прошлой попытки V002 (не была зажурналирована): в проекте висел второй ENABLED-flow `ecxB4AlkUG5FqdvXmjBWv` с тем же именем — создавал дубль-задачу на каждую сделку. `DELETE /v1/flows/…` → 204, его вебхук в amo снялся автоматически (живое подтверждение корректной дерегистрации вебхуков при удалении flow).
- Верификация:
  - Субагент-верификатор (sonnet, свежие глаза) по спекам W005–W013 и диффам: **все 9 задач соответствуют**, security-чек-лист моста — все пункты OK (валидация до логики, 403, CORS-regex, секреты не в логах/reason, исходящие только на форк, cross-tenant по связке). Nits: 500 вместо 403 на чужой CORS-origin (осознанно, W005), «27 vs 28 core» — расхождение текста PRD, не кода.
  - code-review (low) по накопленному диффу блока: **1 находка** (совпала с верификатором) — `contacts/leads/todo.selected` в script.js без `return true` (унаследовано с W001). **Исправлено в этой итерации** (локаций массовых действий в манифесте нет — amo эти коллбеки не зовёт, но конвенция «return в каждом коллбеке» теперь без исключений). `node --check` + grep-линтер после фикса → OK/0.
- Браузерные доборы (виджет: ввод ключа/страница/F5/переходы/lcard/тёмная тема/localStorage-бэкдор) — **НЕ выполнены, внешняя причина**: chrome-devtools MCP недоступен (`ToolSearch select:… → No matching deferred tools`), CDP :9333 отдаёт «amoCRM: Авторизация» (сессия разлогинена; пароли не подбирались — правило). Прецедент W004/W006/W010/W011/W013. Доборы остаются на живую сессию + установку виджета человеком (INSTALL.md).
- Остатки на стенде (безвредно, для сведения): тестовые сделки 38763259/38763265 + задачи на них; origin `w008check.amocrm.ru` в CSP (с W008); flow `j3HDdLic…` включён намеренно (регресс V003+).
- Блокеры: нет.

## 2026-07-12 05:19 — W014 Мост: список flow + приёмник DP-вебхука → запуск flow

- **Что появилось:**
  - `bridge/src/fork-client.ts` — `listEnabledFlows({token,projectId})` (GET `{fork}/api/v1/flows?projectId=…&status=ENABLED&limit=100`, Bearer сессии; 401 → `{ok:false,unauthorized:true}` для сброса кэша; парсит `SeekPage.data`, помечает `webhookCompatible` = триггер `PIECE_TRIGGER` + `pieceName '@activepieces/piece-webhook'` + `triggerName 'catch_webhook'` — сверено с кодом форка) и `runFlowWebhook({flowId,body})` (POST `{fork}/api/v1/webhooks/{flowId}`, async-путь).
  - `bridge/src/flows.ts` (новый) — `GET /flows?install_key&account_id`: сверка активной связки (subdomain из БД, не из запроса), кэш project-сессии в памяти (Map по accountId, TTL 6д из 7 живущих; повторный обмен только по TTL/401 — одна ретрай-попытка со сбросом), отдаёт `[{id,displayName}]` только webhook-совместимых flow. Провижинер-JWT (`PROVISIONER_USER_ID` экспортирован из provision-connection, тот же externalUserId что W009 — не меняет роль клиента).
  - `bridge/src/dp.ts` (новый) — `POST /dp?k=<DP_SECRET>`: сверка `k` + активность account_id → **200 сразу** (`{status:'accepted'}`), затем async `launchFlow`: листинг flow аккаунта → проверка принадлежности flow_id ЭТОМУ аккаунту (чужой → дроп с warn, cross-tenant защита) → `runFlowWebhook` c `{source:'amocrm_dp',event,account_id,subdomain}`. Форма payload — справочник (flow_id в `action.settings.widget.settings.flow_id`).
  - `config.ts`+`env.example` — обязательный env `DP_SECRET` (в манифесте пойдёт в `webhook_url?k=`; у amo-вебхука подписи нет — `k` это наш максимум, зафиксировано). Тесты (jwt/install/embed-token/provision/fork-client) дополнены `DP_SECRET` в env-сетапе.
  - `index.ts` — регистрация `registerFlows`/`registerDp` под префиксом `/bridge`.
- **Команды (фактический результат):** `npx tsc --noEmit` → чисто; `npx vitest run` → **7 файлов, 52/52** (добавлены dp.test 8, flows.test 4, fork-client +5 listEnabledFlows/runFlowWebhook). `code-review` (low) по диффу → находок нет.
- **Живьём (стенд жив, не пересоздавался; форк:8080 + туннель amoai-dev.dzen.team отвечают на `/api/v1/flags`):** мост перезапущен с новым кодом (нужен DP_SECRET — добавлен в `.env`, first6 `gTs44d`, вне git; старый pid 32557 убит, новый слушает :8083, health ok).
  - `GET /bridge/flows` валидной связкой (install_key first6 `RiFRNw`, account 32453394) → **200 `[]`** (обмен JWT→сессия и листинг прошли; включённых webhook-flow в проекте пока нет — не 502). Чужой account_id → **403**; без install_key → **400**.
  - `POST /bridge/dp`: неверный `k` → **403** (форк не тронут); неизвестный account → **403**; payload без flow_id → **400**; валидный `k`+account с чужим flow_id → **200 `{status:'accepted'}`**, в логе async `dp: flow_id not owned by account` (cross-tenant дроп подтверждён живьём).
  - Прямой `POST {fork}/api/v1/webhooks/nonexistent` → **400 FST_ERR_VALIDATION** (`flowId` обязан быть apId `^[0-9a-zA-Z]{21}$`) — webhook-эндпоинт форка жив, формат ID зафиксирован.
- **Добор (не блокер):** реальный запуск flow-run через DP («flow-run появился в проекте») требует включённого `catch_webhook`-flow в проекте dzenteamdev, которого сейчас нет (GET /flows → `[]`). Механика запуска подтверждена: unit `runFlowWebhook` (POST на корректный URL), живой webhook-эндпоинт форка (400 на невалидный ID), сквозной DP-путь до дропа. Реальный запуск добирается на **W015** (её verify живьём строит автоворонку с шагом → выбор flow → переход этапа → flow запустился; фактический DP payload тогда же сверяется со справочником).
- Блокеры: нет.

## 2026-07-12 05:26 — W015 Manifest DP-блок + dpSettings-плашка выбора flow

- **Что появилось:**
  - `widget/manifest.json` — `digital_pipeline` в `locations`; блок `dp`: `settings.flow_id {name:'settings.flow_id', type:'text', required:true}`, `action_multiple:false`, `webhook_url:'https://amoai-dev.dzen.team/bridge/dp?k=__DP_SECRET__'`; version 0.1.1→0.1.2.
  - **Секрет DP вне git:** в committed-манифесте placeholder `__DP_SECRET__`; `build.sh` подставляет реальный `DP_SECRET` (env или `../bridge/.env`) только в zip через staging-каталог (`mktemp -d` + `sed` + zip оттуда, trap-очистка). Нет `DP_SECRET` → build падает с внятным сообщением. Совпадение с `config.dpSecret` моста обязательно (иначе amo→мост 403).
  - `bridge/static/widget/widget-app.js` — `dpSettings()`: находит `#widget_settings__fields_wrapper input[name="flow_id"]` (механика записи в поле шага — из `reference/triggeron/widget/vendor/controller/DpSettings.js`: amo рендерит поля dp.settings как `<input name=код>`, сохраняет их значение), прячет сырой input, ставит `<select.dzenflow-dp-select>` из `GET /flows?install_key&account_id`, на change пишет id обратно в input + `.trigger('change')`. Гард `data('dzenflowEnhanced')` от повторного select. Нет ключа/аккаунта → плашка в опции; сеть/ошибка → input остаётся видимым (graceful degrade). Имена flow через `new Option(text,val)` (textContent, не HTML). Экспорт `dpSettings` в фабрике.
  - `widget/script.js` — коллбек `dpSettings` (делегирует в app, `return true`).
  - `widget/i18n/ru.json` — `settings.flow_id` + блок `dp.{loading,choose,empty,no_key,error}`.
  - `widget/images/logo_dp.png` — 174×109 (sips из logo.png), 1210 байт (<300КБ).
- **Команды (фактический результат):** `node --check script.js` OK; `node --check widget-app.js` OK; grep-линтер (console/alert/confirm/prompt) обоих =0; `node -e require(manifest+i18n)` OK; `./build.sh` → zip собран (11 файлов, logo_dp внутри), в zip-манифесте placeholder заменён (`placeholder_left:false`, `k_first6:gTs44d` = DP_SECRET моста), zip gitignored. `tsc --noEmit` чисто; `vitest run` 7 файлов 52/52 (TS не трогал — регресс-проверка). `code-review` (low) по диффу → находок нет.
- **Живьём (стенд жив, не пересоздавал; local8080/tunnel/bridge-health = 200):** `GET /bridge/flows` валидной связкой (install_key first6 `RiFRNw`, account 32453394) → **200 `[]`** (источник данных для select подтверждён; включённых webhook-flow в проекте нет → select покажет «Нет доступных сценариев»); без install_key → **400**.
- **Ожидает установки (не блокер):** коллбек `dpSettings` и живой DP-payload срабатывают только у УСТАНОВЛЕННОГО виджета с `digital_pipeline` в манифесте — текущая установка (widget_code `dealguard`, v0.1.1) DP-блока не имеет; нужна перезаливка zip v0.1.2 человеком (INSTALL.md, с подстановкой DP_SECRET через build.sh). После установки добираются: (1) рендер select в редакторе шага автоворонки; (2) фактический DP-payload от amo (сверка со справочником/W014 dp.ts); (3) сквозной запуск flow при переходе этапа. Пометка для V003.
- Секрет-скан диффа (JWT-префикс + PEM-заголовок) = 0; в диффе манифеста только placeholder `__DP_SECRET__`, реальный секрет живёт в bridge/.env. `git status`: изменены только разрешённые пути (widget/**, bridge/static/**); logo_dp.png untracked (добавляется в коммит).
- Блокеры: нет.
